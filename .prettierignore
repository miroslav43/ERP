.next
node_modules
pnpm-lock.yaml
docs/design
src/types/database.ts

# Director de lucru al plugin-ului remember — ignorat de git prin
# .remember/.gitignore, deci n-are ce căuta în verificarea de formatare.
.remember

# Aplicația mobilă are propriul lanț de unelte și propriul lockfile.
mobil/

# Director de lucru al plugin-ului superpowers. E exclus din git prin
# `.git/info/exclude`, care e LOCAL și necomis — deci CI nu vede fișierele, dar
# `prettier --check .` de pe mașina cuiva le vede și raportează 20 de avertismente
# care nu există în CI. Ignorarea aliniază cele două verdicte.
.superpowers
