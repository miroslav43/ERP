## idempotenta
VERDICT: Motorul are scheletul corect — cheia de identitate cu `kind`, `due_date` copiat din `expires_at` (deci stabil între rulări, fără derivă de fus), `on conflict do nothing` pentru generare şi UPDATE-uri convergente la curăţare — iar rulările concurente nu produc duplicate la nivel de constrângere: PostgreSQL serializează inserţiile pe indexul unic, deci cron întârziat + declanşare manuală simultană nu dublează alertele. Fusul orar este tratat corect: `app.azi_local()` este singura sursă de „azi”, iar decalajul Bucureşti/UTC nu poate muta un document cu o zi la ora de rulare aleasă. Problemele reale sunt în semantica ciclului de viaţă, nu în deduplicare. Trei dintre ele sunt blocante pentru livrare: (1) o alertă ajunsă `rezolvat` nu se mai poate regenera niciodată pentru aceeaşi cheie, deci un document expirat pe care cineva a apăsat „Rezolvă” devine invizibil pentru motor pe termen nelimitat — promisiunea centrală a modulului cade tăcut; (2) `curata_alerte` retrogradează în fiecare noapte alertele confirmate şi depăşite înapoi la `expirat`, ceea ce anulează la infinit confirmarea umană; (3) cursa între `sync_expirable` şi `genereaza_alerte` (snapshot READ COMMITTED) lasă alerte fantomă pentru scadenţe deja reînnoite, iar nimic din sistem nu reconciliază ulterior `due_date` cu `expires_at`. La acestea se adaugă două căi reale de dublare a e-mailului — pierderea stabilităţii lui `expirable_id` la editarea în doi paşi şi lipsa unui claim atomic între rezervare şi trimitere — plus inundarea jurnalului de audit de către polling-ul clopoţelului. Corectările sunt punctuale şi nu ating decizia de arhitectură (tabelă materializată în locul unei view cu UNION), care rămâne bine argumentată; recomand implementarea frazei de reconciliere din constatarea 3, fiindcă ea singură închide şi o parte din efectele constatărilor 1 şi 4.
- [CRITIC] 0006_expirables.sql
  PROBLEMA: Indexul de deduplicare `compliance_alerts_dedup_uq (expirable_id, prag_zile, due_date)` este TOTAL, iar `genereaza_alerte` face `on conflict do nothing`. O alertă ajunsă în starea `rezolvat` blochează pentru totdeauna regenerarea aceleiaşi combinaţii. Scenariu: ITP-ul unui vehicul expiră pe 2026-09-01, jobul creează alerta de depăşire (prag 0, due 2026-09-01), operatorul apasă „Rezolvă” ca să golească inbox-ul fără să reînnoiască actul. Din acel moment vehiculul este expirat la infinit, dar nu mai apare NICIODATĂ o alertă şi nu mai pleacă niciun e-mail — exact promisiunea produsului cade tăcut. Varianta a doua, la fel de reală: data se tastează greşit (09-10), `sync_expirable` închide automat alertele pentru 09-01 ca „rezolvat”, apoi utilizatorul corectează înapoi la 09-01 — cheia există deja ca rezolvată, deci nu se mai generează nimic.
  CORECTIE: Separă „închis pentru că s-a rezolvat documentul” de „închis pentru că alerta nu mai are obiect”: adaugă o coloană `inchisa_de_masina boolean` sau, mai curat, fă indexul unic parţial `where status <> 'rezolvat' or deleted_at is not null` NU e suficient — soluţia corectă este ca `genereaza_alerte` să facă `on conflict (...) do update set status = case when public.compliance_alerts.status = 'rezolvat' and excluded.due_date >= app.azi_local() ... end` sau, mai simplu, să REDESCHIDĂ alertele închise automat: `do update set status = excluded.status, resolved_at = null, resolved_by = null where public.compliance_alerts.resolved_by is null` (adică doar cele închise de maşină, nu cele închise de om), iar pentru cele închise de om verifică suplimentar că `expires_at` chiar s-a schimbat înainte de a le considera stinse.
- [MARE] 0006_expirables.sql
  PROBLEMA: `internal.curata_alerte` — primul UPDATE are predicatul `a.status in ('nou','confirmat') and a.due_date < v_azi` şi forţează `status = 'expirat'`. Asta distruge confirmarea umană în fiecare noapte, la nesfârşit. Scenariu: ITP expirat pe 1 septembrie; pe 2 septembrie şeful de parc apasă „Am văzut” (status → `confirmat`); în noaptea de 3 septembrie jobul îl întoarce la `expirat`; omul confirmă din nou; a doua noapte îl întoarce iar. Ping-pong permanent pe exact categoria de alerte care rămâne cel mai mult în sistem (cele depăşite). Butonul „Am văzut” este funcţional inutil, iar `acknowledged_at` rămâne setat deşi statusul spune contrariul.
  CORECTIE: Restrânge predicatul la `a.status = 'nou'` (o alertă confirmată de un om nu se mai retrogradează automat), sau mută semnalul de depăşire în afara coloanei `status` — de exemplu un `este_depasita boolean generated always as (due_date < ...)` nu se poate (nu e IMMUTABLE), deci o coloană separată `depasita_la date` actualizată de job, lăsând `status` să reflecte doar decizia umană.
- [MARE] 0006_expirables.sql
  PROBLEMA: Cursă reală între `internal.sync_expirable` şi `internal.genereaza_alerte`, iar nimic nu o repară ulterior. În READ COMMITTED, INSERT-ul din `genereaza_alerte` îşi ia snapshot-ul la începutul comenzii. Scenariu: jobul de noapte (sau o declanşare manuală) rulează, citeşte `expires_at = 2026-09-01`; în acelaşi moment un utilizator salvează fişa vehiculului cu ITP nou 2027-09-01, iar `sync_expirable` închide alertele cu `due_date = 2026-09-01` — dar comanda de INSERT a jobului comite DUPĂ acea închidere şi scrie o alertă nouă pentru data veche. Rezultatul: o alertă fantomă pentru un document deja reînnoit, care nu va fi închisă niciodată — `curata_alerte` doar o va marca `expirat` (due_date < azi) şi va rămâne roşie permanent în inbox. Acelaşi efect apare la orice reînnoire ratată de blocul `if v_expires_vechi <> p_expires_at`.
  CORECTIE: Adaugă în `internal.curata_alerte` o a treia frază de reconciliere, care nu depinde de momentul reînnoirii: `update public.compliance_alerts a set status='rezolvat', resolved_at=now(), updated_at=now() from public.expirables e where e.id = a.expirable_id and a.deleted_at is null and a.status in ('nou','confirmat','expirat') and a.due_date <> e.expires_at;`. Este idempotentă şi închide deterministic orice alertă a cărei scadenţă nu mai corespunde sursei, indiferent de curse.
- [MARE] 0006_expirables.sql
  PROBLEMA: Cheia de deduplicare a alertelor este `expirable_id`, dar `expirable_id` NU este stabil: la `p_expires_at is null` funcţia `sync_expirable` face soft delete, iar indexul unic al scadenţelor este parţial (`where deleted_at is null`), deci o resincronizare ulterioară creează un rând NOU, cu alt `id`. Scenariu banal de birou: operatorul editează fişa în doi paşi — şterge data ITP şi salvează (scadenţa e ştearsă logic, alertele închise), apoi introduce data corectă şi salvează din nou (scadenţă nouă, id nou). Jobul regenerează întregul set de alerte (prag 30/14/7) şi, pentru că `alert_notifications` deduplică pe `alert_id`, trimite din nou toate e-mailurile aceluiaşi responsabil pentru acelaşi document.
  CORECTIE: Nu şterge logic rândul la `p_expires_at is null`; păstrează-l cu `expires_at` neschimbat şi marchează-l inactiv (`is_active=false`) sau adaugă o coloană `scadenta_activa`. Alternativ, la re-creare reutilizează rândul şters (`update ... set deleted_at = null` pe rândul cu aceeaşi identitate) în loc de a insera unul nou, astfel încât `expirable_id` să rămână stabil pe toată durata de viaţă a entităţii sursă.
- [MARE] supabase/functions/conformitate-zilnica/trimite.ts
  PROBLEMA: Garanţia declarată în secţiunea 14 („inserarea rezervă dreptul de a trimite ÎNAINTE de trimitere, deci o repornire la mijlocul fazei nu poate dubla e-mailul”) acoperă doar o direcţie. Rândul `alert_notifications` se comite cu `status='in_asteptare'`, apoi se trimite, apoi se marchează `trimis`. Dacă furnizorul de e-mail acceptă mesajul şi procesul cade / expiră timeout-ul înainte de UPDATE, rândul rămâne `in_asteptare` şi `alert_notifications_coada_idx` îl oferă din nou la următoarea rulare → acelaşi rezumat pleacă a doua oară către acelaşi om. La un cron zilnic cu retry, asta se vede ca dublură sistematică în zilele cu erori de reţea.
  CORECTIE: Fă rezervarea condiţionată de un token: la preluare, `update ... set status='in_curs', updated_at=now() where status='in_asteptare' returning id` (claim atomic), apoi persistă identificatorul de mesaj întors de furnizor în `email_log_id` / o coloană `provider_message_id` ÎNAINTE de a considera trimiterea încheiată; la repornire, rândurile `in_curs` mai vechi de N minute se verifică la furnizor după acel identificator în loc să fie retrimise orbeşte.
- [MARE] src/app/(app)/notificari/actions.ts
  PROBLEMA: `listeazaNotificari` are `audit: { action: 'view', entityType: 'notification', allow: [] }`, iar `ClopotelNotificari` o apelează la montare şi apoi la fiecare 60 de secunde (`INTERVAL_REIMPROSPATARE_MS = 60_000`), în fiecare tab deschis. Scenariu: 20 de utilizatori cu aplicaţia deschisă 8 ore înseamnă ~9.600 de rânduri `audit_logs` pe zi care nu conţin nicio decizie umană. Jurnalul de audit — artefactul pe care se sprijină controlul — devine imposibil de citit în câteva săptămâni şi creşte necontrolat.
  CORECTIE: Scoate complet auditul de pe acţiunea de listare (citirea propriei căsuţe nu este un eveniment auditabil) sau, dacă auditul lecturii e cerut, mută-l pe o acţiune explicită de deschidere a paginii, nu pe polling. În plus, înlocuieşte polling-ul la 60 s cu `revalidate`/Realtime, sau ridică intervalul substanţial.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `destinatar_cheie` este `coalesce(recipient_user_id::text, recipient_employee_id::text, 'organizatie')` — deduplică pe identitatea rândului, nu pe adresa de livrare. Scenarii care produc două e-mailuri aceluiaşi om pentru aceeaşi alertă: (a) o rulare rezolvă responsabilul prin contul de portal (`recipient_user_id`) şi alta prin fişa de angajat (`recipient_employee_id`) — chei diferite, ambele inserate; (b) doi angajaţi împart aceeaşi adresă de firmă (frecvent la IMM-uri: birou@firma.ro) — două rânduri, două e-mailuri identice; (c) invers, mai multe destinaţii care cad pe fallback-ul `'organizatie'` colapsează într-un singur rând şi doar unul dintre destinatari e înregistrat ca notificat.
  CORECTIE: Adaugă o coloană `destinatar_adresa text` (adresa normalizată, lowercase/trim) şi construieşte `destinatar_cheie` din `canal || ':' || coalesce(lower(destinatar_adresa), recipient_user_id::text, ...)`, astfel încât unicitatea să fie pe canalul de livrare efectiv, nu pe rândul din care a fost dedus.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: CTE-ul `praguri` foloseşte `c.expires_at <= v_azi + p.prag`, deci pentru un document aflat deja în interiorul ferestrei se potrivesc TOATE pragurile deodată. Scenariu: se introduce astăzi un contract care expiră peste 5 zile, cu pragurile implicite 30/14/7 — jobul creează instantaneu trei alerte pentru acelaşi document, iar rezumatul pe e-mail listează documentul de trei ori. Acelaşi lucru se întâmplă pentru toate scadenţele din fereastră la prima rulare după o pană a cron-ului.
  CORECTIE: Generează doar pragul cel mai mic încă neatins: în CTE, în loc de `cross join lateral unnest`, alege `min(prag) filter (where c.expires_at <= v_azi + prag)` per scadenţă, sau filtrează suplimentar cu `not exists (select 1 from public.compliance_alerts a where a.expirable_id = c.id and a.due_date = c.expires_at and a.prag_zile < p.prag)`. Robusteţea la zile ratate se păstrează, fiindcă predicatul rămâne `<=`, nu `=`.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `internal.sync_expirable` suprascrie necondiţionat `nota` la închiderea automată (`nota = 'Închisă automat: ...'`), în timp ce `internal.curata_alerte` foloseşte `coalesce(a.nota, ...)`. Scenariu: responsabilul scrie în notă „am programare la RAR pe 12.09, dosarul e la contabilitate”; documentul se reînnoieşte şi textul introdus de om dispare definitiv, fără urmă în interfaţă. Cele două funcţii ale aceluiaşi motor tratează diferit acelaşi câmp.
  CORECTIE: Uniformizează: în ambele funcţii foloseşte fie `coalesce(a.nota, '<mesaj automat>')`, fie concatenare cu păstrarea notei umane, fie mută mesajele maşinii într-o coloană separată `nota_sistem text`, lăsând `nota` exclusiv pentru text introdus de utilizator.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `internal.compliance_alerts_valideaza` setează `new.acknowledged_at := now()` ori de câte ori statusul devine `confirmat` sau `rezolvat`, inclusiv când închiderea este făcută de maşină (`sync_expirable` şi `curata_alerte` scriu `status='rezolvat'` fără `acknowledged_by`). Scenariu: la un control, raportul arată alerte cu `acknowledged_at` completat şi `acknowledged_by` NULL — pare o confirmare umană fără autor. Într-un modul de conformitate, o urmă falsă de „cineva a văzut” este mai rea decât absenţa ei.
  CORECTIE: Setează `acknowledged_at` doar când există şi un autor: `if new.status in ('confirmat','rezolvat') and new.acknowledged_by is not null and new.acknowledged_at is null then ...`. Pentru închiderile automate lasă ambele câmpuri NULL şi marchează-le prin `resolved_by is null` + nota de sistem.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `app.dashboard_conformitate` returnează `min(e.expires_at)` pe grupul întreg, inclusiv rândurile deja expirate, dar coloana se numeşte `cea_mai_apropiata` şi este afişată în interfaţă sub antetul „Cea mai apropiată”. Scenariu: o categorie cu un ITP expirat din 2023 şi zece documente valabile în octombrie 2026 va afişa „15.03.2023” drept cea mai apropiată scadenţă — cifra pe care administratorul o citeşte primul este lipsită de sens şi îl trimite pe pistă greşită.
  CORECTIE: `min(e.expires_at) filter (where e.expires_at >= app.azi_local())` pentru „cea mai apropiată”, plus, dacă se doreşte, o coloană separată `cea_mai_veche_expirata` cu `min(...) filter (where e.expires_at < app.azi_local())`.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `alert_rules.valabil_de_la` are `default current_date`, care se evaluează în fusul sesiunii (UTC pe Supabase), în timp ce întregul motor foloseşte `app.azi_local()` (Europe/Bucharest). Scenariu vara, la 02:30 ora României (23:30 UTC ziua precedentă): administratorul salvează o regulă fără să completeze data, iar rândul primeşte data de IERI. Dacă există deja o versiune cu acea dată, INSERT-ul loveşte `alert_rules_versiune_uq` şi utilizatorul primeşte „Există deja o regulă ... Alegeţi altă dată”, deşi nu a ales nicio dată. În plus, nu există nicio acţiune de UPDATE pe `alert_rules`: o regulă creată azi nu mai poate fi corectată azi, prin niciun drum din interfaţă.
  CORECTIE: Înlocuieşte `default current_date` cu `default app.azi_local()` (funcţia e STABLE, deci utilizabilă ca default) şi adaugă o acţiune de corecţie pentru versiunea curentă — fie un `update` restrâns la ziua curentă, fie soft delete al versiunii greşite plus inserarea celei corecte, în aceeaşi tranzacţie.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `internal.sync_expirable` setează `app.sincronizare_expirari = 'on'` şi îl readuce la 'off' doar pe calea de succes, prin `perform set_config(...)` la final. Dacă apare o excepţie între cele două apeluri (violare de CHECK pe `label`, `entity_type` care nu trece regex-ul, FK invalid pe `responsible_employee_id`) ŞI apelantul o prinde într-un bloc `exception when others then` — tipar frecvent în triggerele de backfill — flagul rămâne 'on' pentru tot restul tranzacţiei, iar `internal.expirables_protejeaza` nu mai apără nimic: orice UPDATE ulterior din aceeaşi tranzacţie poate schimba `expires_at`, `is_active` sau `organization_id` fără să fie oprit.
  CORECTIE: Împachetează corpul funcţiei într-un bloc `begin ... exception when others then perform set_config('app.sincronizare_expirari','off',true); raise; end;`, sau, mai robust, elimină flagul global şi mută scrierile în tabelă într-o funcţie internă separată pe care triggerul o recunoaşte după `pg_trigger_depth()` / după rolul curent.
- [MEDIU] 0006_expirables.sql
  PROBLEMA: `internal.sync_expirable` nu închide alertele când entitatea sursă devine inactivă (`p_is_active = false`); asta rămâne exclusiv în sarcina lui `internal.curata_alerte`, care rulează o dată pe noapte. Scenariu: vehiculul este casat luni dimineaţa; semaforul din dashboard îl exclude imediat (interogarea filtrează `is_active`), dar inbox-ul de alerte şi e-mailul de rezumat continuă să îl raporteze roşu până marţi dimineaţa. Dacă jobul zilnic este oprit (pană pg_cron, funcţie Edge nedesfăşurată), alertele pentru flota vândută rămân la nesfârşit, iar `is_active` nu are niciun efect asupra lor. În plus, `p_is_active boolean default true` combinat cu `is_active = excluded.is_active` face ca orice apelant care omite parametrul să reactiveze o scadenţă dezactivată anterior.
  CORECTIE: În `sync_expirable`, când `coalesce(p_is_active,true) = false`, închide imediat alertele deschise ale scadenţei (acelaşi UPDATE ca la ramura `p_expires_at is null`). Pentru resurecţie, schimbă `p_is_active` în `boolean default null` şi foloseşte `is_active = coalesce(excluded.is_active, e.is_active)`, ca omiterea parametrului să însemne „nu ştiu”, nu „activ”.

## securitate
VERDICT: RESPINS — nu se poate aplica în forma actuală. Trei categorii de probleme: (1) blocaj total — toate politicile RLS noi apelează `app.has_permission` cu semantica inventată `(org, 'resursa:actiune', scope)`, în timp ce funcția reală din 0002_authz.sql este `(org, resource, action) returns permission_scope`; policy-urile nu se creează (eroare de tip la `and`), deci migrarea nu rulează. (2) Izolare — `expirables` este polimorfă, dar SELECT-ul reevaluează doar `compliance:read`, nu regula tabelei sursă: o fișă de aptitudine medicală marcată `confidential=true`, refuzată de `employee_documents_select`, devine vizibilă prin proiecție oricui are conformitate la scope 'team'; `label`-ul liber duce informația mai departe în email și în `email_log.subiect` (citibil cu `audit:read='all'`). (3) Integritate operațională — o alertă poate fi ștearsă logic prin PostgREST și, din cauza indexului de dedup total, nu mai poate fi regenerată niciodată; `acknowledged_by`/`resolved_by` nu sunt fixate la `auth.uid()`; regulile pot fi mutate între firme; iar declanșatorul jobului (ruta cron și secretul) pur și simplu nu există — `serverEnv` nu are nici `ALERTE_CRON_SECRET`, nici `APP_URL`. De rezolvat, în ordine: 1, 2, 3, 4, 5, 11, apoi restul.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Toate politicile RLS noi apelează `app.has_permission(organization_id, 'compliance:read', 'all')`. Semnătura reală (0002_authz.sql:189) este `app.has_permission(p_organization_id uuid, p_resource text, p_action text) returns public.permission_scope`. Deci: (a) se caută resource='compliance:read' + action='all', combinație care nu există în seed → mereu 'none'; (b) rezultatul este un enum folosit direct într-un `and` → `ERROR: argument of AND must be type boolean, not type permission_scope` la `create policy`. Migrarea NU se aplică deloc; nu e o degradare, e un blocaj total.
  CORECTIE: Folosește tiparul existent din 0005/0007: `case app.has_permission(organization_id,'compliance','read') when 'all' then true when 'team' then app.is_manager_of(organization_id, responsible_employee_id) when 'own' then responsible_employee_id = app.current_employee_id(organization_id) else false end`, iar pentru praguri binare `app.can(organization_id,'compliance','update','all')`.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: `expirables` este polimorfă, dar `expirabile_select` reevaluează DOAR `compliance:read` — nu permisiunea entității sursă. În 0005_hr_rls.sql:562, `employee_documents_select` cere `employees:read = 'all'` SAU (`can_see_employee` ȘI `confidential = false` ȘI `vizibil_angajatului = true`). Scenariu concret: firma acordă unui manager `compliance:read = 'team'` (exact cazul pentru care e scrisă ramura 'team'); prin `/conformitate/alerte` acesta vede scadența, tipul și data unei fișe de aptitudine medicală marcate `confidential = true` pentru subordonații lui — date pe care tabela sursă i le refuză explicit. Proiecția devine un canal lateral care ocolește regula documentului.
  CORECTIE: Adaugă în `expirables` coloanele `source_resource text not null` + `source_action text not null default 'read'` (completate de `sync_expirable`) și scrie politica de SELECT ca reevaluare a permisiunii sursei: `case app.has_permission(organization_id, source_resource, source_action) ... end`, cu ramurile de scope legate de `responsible_employee_id` prin `app.can_see_employee`. Orice `entity_type`/`source_resource` necunoscut = refuz implicit (else false).
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: `label` este text liber (200 caractere) copiat din modulul sursă și devine informația însăși: „Fișă de aptitudine — Popescu Ana, inapt”. El se afișează în listă, în emailul de alertă și ajunge în `email_log.subiect`, tabelă citibilă de oricine are `audit:read='all'` (0002_authz.sql:1119) — altă permisiune decât cea care protejează documentul medical. Datele de sănătate (art. 9 GDPR) ies astfel din perimetrul RLS-ului care le protejează.
  CORECTIE: `label` se generează neutru, din `kind` + o referință nepersonală (ex. „Fișă de aptitudine — marca 1234”); numele și detaliile se rezolvă la afișare, printr-o citire separată din tabela sursă, sub permisiunea acesteia. În email trimite doar tipul, data și un link intern — niciodată eticheta brută. Adaugă un CHECK/normalizare care refuză etichetele generate din câmpuri libere pentru `entity_type` din zona HR/medical.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Suprimare permanentă a unei alerte. `alerte_update` are `with check (organization_id = any(...))` — fără `deleted_at is null` — iar `compliance_alerts_dedup_uq` este intenționat TOTAL. Utilizatorul are cheia anon + JWT propriu, deci poate apela PostgREST direct: `PATCH /rest/v1/compliance_alerts?id=eq.<x>` cu `{"deleted_at":"now()"}`. Alerta dispare din inbox, iar `internal.genereaza_alerte` nu o mai poate recrea niciodată (ON CONFLICT DO NOTHING pe indexul total). Un ITP expirat sau o autorizație de muncă depășită se pot ascunde definitiv, iar dashboardul rămâne verde.
  CORECTIE: Adaugă `deleted_at is null` în WITH CHECK-ul lui `alerte_update` (ștergerea logică rămâne exclusiv în context de serviciu) și, complementar, fă indexul de dedup parțial `where deleted_at is null` doar dacă ștergerea devine imposibilă pentru `authenticated`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Trasabilitate falsificabilă și, în practică, inexistentă. Nici politica, nici `internal.compliance_alerts_valideaza()` nu fixează `acknowledged_by` / `resolved_by`; acțiunile din `alerte/actions.ts` nu le setează nici ele. Rezultat: pe calea normală coloanele rămân NULL (deci „cine a confirmat, cine a rezolvat” din comentariul secțiunii 13 nu se materializează), iar prin PostgREST direct un utilizator cu `compliance:update` poate trimite `{"status":"rezolvat","resolved_by":"<id-ul unui coleg>"}` — registrul de conformitate arată că altcineva a închis alerta.
  CORECTIE: În trigger: `if new.status in ('confirmat','rezolvat') then new.acknowledged_by := coalesce(old.acknowledged_by, (select auth.uid())); end if;` și analog pentru `resolved_by`; în WITH CHECK impune `acknowledged_by is not distinct from (select auth.uid()) or (select app.is_service_context())`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Mutare cross-tenant a regulilor de alertare. `reguli_update` verifică în WITH CHECK doar `organization_id = any((select app.current_org_ids())::uuid[])` — nu egalitatea cu organizația rândului vechi și nici permisiunea în organizația destinație — iar `alert_rules` nu are trigger care să blocheze schimbarea lui `organization_id` (spre deosebire de `expirables` și `compliance_alerts`, unde coloana e protejată). Un utilizator `org_admin` în firma A și simplu `employee` în firma B execută `PATCH /rest/v1/alert_rules?id=eq.<regula din A>` cu `{"organization_id":"<B>"}` și rescrie pragurile de alertare ale firmei B, unde nu are nicio permisiune de conformitate.
  CORECTIE: Adaugă un trigger BEFORE UPDATE pe `alert_rules` care refuză modificarea lui `organization_id` (P0001), și dublează în WITH CHECK: `app.can(organization_id,'compliance','update','all')` — nu doar apartenența.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: `expirabile_update` nu îngustează după scope: USING cere doar `compliance:update` la 'team', fără condiția de responsabil/manager din politica de SELECT. Un șef de echipă poate rescrie `notes` (2000 de caractere, afișate altora) și reatribui `responsible_employee_id` pe rânduri pe care nu are dreptul să le vadă — reatribuindu-le, de altfel, către el însuși, ceea ce i le face ulterior vizibile prin ramura 'own'/'team'. Cum `internal.attach_audit('expirables')` nu se apelează (secțiunea 13 exclude tabela deliberat), modificarea nu lasă nicio urmă.
  CORECTIE: Replică în USING exact ramurile de scope din `expirabile_select` (responsabil = eu / `app.is_manager_of`) și adaugă `select internal.attach_audit('expirables');` — motivația „proiecție generată de mașină” cade din moment ce politica permite editare umană.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Contradicție în calea de scriere. `expirabile_insert` cere `app.is_service_context()`, care — conform măsurătorii documentate chiar în 0002_authz.sql:277 — citește GUC-ul `role` și rămâne FALS în interiorul unei funcții SECURITY DEFINER apelate de un utilizator `authenticated`. Dar `internal.sync_expirable` este documentată ca fiind apelată din triggerele modulelor la salvarea entității („le creează salvând vehiculul”). Pe orice mediu unde proprietarul tabelei nu ocolește RLS (FORCE e activ, CI local), salvarea vehiculului va eșua cu „new row violates row-level security policy for table expirables”, blocând scrierea legitimă a utilizatorului; acolo unde proprietarul ocolește RLS, politica este cod mort și singura gardă rămâne verificarea din corpul funcției.
  CORECTIE: Alege o singură cale și scrie-o explicit: fie sincronizarea se face exclusiv din jobul de serviciu (și atunci triggerele modulelor doar marchează entitatea ca „de resincronizat”), fie politica de INSERT se bazează pe `pg_trigger_depth() > 0 and app.can(organization_id, <resursa sursei>, 'update', 'own')`. Testează ambele medii cu `set local role authenticated`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/notificari/actions.ts
  PROBLEMA: Coloane inexistente: `public.notifications` (0001_kernel.sql:373) are `title`, `body`, `kind`, `link`, `read_at`, `deleted_at`, iar `user_id` referă `auth.users`, nu `profiles`. Interogarea `select("id, title, message, link, read_at, created_at")` întoarce PostgREST 42703 la fiecare încărcare a paginii `/notificari` și la fiecare tick al clopoțelului. În plus lipsește `.is('deleted_at', null)`, iar `marcheazaToateCitite` atinge și rândurile șterse logic.
  CORECTIE: `message` → `body` (și `mesaj: n.body ?? ""`, coloana e nullable), adaugă `.is('deleted_at', null)` în toate cele trei acțiuni, iar `alert_notifications.recipient_user_id` poate rămâne pe `profiles(id)` (PK-ul referă la rândul lui `auth.users`).
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/notificari/actions.ts
  PROBLEMA: Gate greșit pe propria căsuță de notificări. Cele trei acțiuni cer `compliance:read`, dar seed-ul din 0002_authz.sql:1137-1147 acordă resursa `compliance` DOAR rolurilor `super_admin` și `org_admin`. Un `manager` sau un `employee` primește refuz (`AccesRestrictionat`) pe `/notificari` și pe clopoțel, deși politica `notifications_select` i-ar permite accesul (`user_id = auth.uid()`). Consecință directă asupra scopului Fazei 4: exact oamenii responsabili operațional nu-și văd alertele.
  CORECTIE: Propriile notificări nu se protejează printr-o permisiune de modul — filtrul `user_id = ctx.user.id` + RLS-ul existent SUNT autorizarea. Folosește o permisiune pe care o are orice membru (sau introdu resursa `notifications` în `PERMISSION_KEYS` și în seed, cu `own` pentru toate rolurile) și elimină verificarea `meetsScope(compliance:read)` din `page.tsx`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/config/env.ts
  PROBLEMA: Declanșatorul jobului nu este apărat de nimic verificabil: ruta `src/app/api/cron/alerte-zilnice/route.ts` este invocată în livrare, dar nu există în repo (`src/app/api` conține doar `export/audit` și `webhooks/resend`), iar `serverEnv` nu conține nici `ALERTE_CRON_SECRET`, nici `APP_URL` — există doar `NEXT_PUBLIC_APP_URL` în `clientEnv`. Deci secretul comparat de rută este `undefined` (orice implementare de forma `token === serverEnv.ALERTE_CRON_SECRET` devine o comparație cu undefined, iar variantele cu `String(...)`/`??''` se satisfac cu antet gol), iar URL-urile din email se construiesc din `undefined`.
  CORECTIE: Adaugă în `serverEnv`: `ALERTE_CRON_SECRET: z.string().min(32)` (fail-fast la boot, fără default) și `APP_URL: z.url()`. În rută: citește antetul, respinge imediat dacă lipsește sau are altă lungime, compară cu `crypto.timingSafeEqual` peste `sha256` al ambelor capete (egalizează lungimea și nu scurge dimensiunea), aplică `consumeRateLimit` pe IP și răspunde 401 fără detalii.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Portiță cross-tenant pentru administratorul de platformă: toate cele patru politici de SELECT încep cu `(select app.is_platform_admin()) or (...)`, în afara filtrului de organizație. Operatorul SaaS citește astfel scadențele (inclusiv etichetele medicale) oricărei firme fără să fie membru. Tabelele HR din 0005_hr_rls.sql nu au această portiță: acolo `organization_id = any((select app.current_org_ids())::uuid[])` este obligatoriu, iar calitatea de admin de platformă se manifestă doar indirect, prin `app.has_permission` care întoarce 'all'.
  CORECTIE: Aliniază la modelul HR: scoate ramura `is_platform_admin()` de deasupra filtrului de organizație și lasă permisiunea să facă diferența; dacă e nevoie de acces de platformă, treacă printr-o funcție auditată, ca `hr_read_sensitive`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Garda anti-modificare din `internal.expirables_protejeaza()` se sprijină pe un GUC obișnuit (`app.sincronizare_expirari`), nu pe `app.is_service_context()`. `set_config` este executabilă de `public`, deci orice cale care ajunge să execute SQL cu rolul apelantului dezactivează garda. În plus, `sync_expirable` setează steagul pe 'on' și îl resetează abia la final: dacă funcția aruncă excepție la mijloc, iar triggerul modulului apelant o prinde într-un bloc `exception`, steagul rămâne 'on' până la sfârșitul tranzacției, iar UPDATE-urile ulterioare ale utilizatorului pot rescrie `expires_at`, `entity_id` sau `organization_id`.
  CORECTIE: Înlocuiește condiția cu `app.is_service_context() or pg_trigger_depth() > 1`, fără GUC. Dacă steagul rămâne, resetează-l într-un bloc `exception when others then perform set_config(...,'off',true); raise;`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: `internal.sync_expirable` nu verifică apartenența lui `p_responsible_employee_id` la `p_organization_id`, iar `expirabile_update` permite setarea coloanei la orice uuid din `employees`; cheia străină este pe `employees(id)`, nu pe `(organization_id, id)`. Se ajunge la rânduri din firma A care referă un angajat al firmei B — vizibile în diagnostic/export, și care rup raportarea „scadențele de care răspund eu”.
  CORECTIE: În funcție: `if p_responsible_employee_id is not null and not exists (select 1 from public.employees e where e.id = p_responsible_employee_id and e.organization_id = p_organization_id and e.deleted_at is null) then raise exception ... end if;`. În tabelă: cheie unică `(organization_id, id)` pe `employees` și FK compusă `(organization_id, responsible_employee_id)`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: `parametri.entityType` și `parametri.kind` ajung nevalidate din query string direct în `interogheazaAlerteConformitate` (fișierul `src/lib/queries/conformitate.ts` nu există în repo și nu a fost livrat, deci graniță nerevizuibilă). Doar `status` este verificat pe listă albă. Dacă interogarea folosește forma text `.or(...)`/`.filter(...)` — singura care suportă filtrarea pe relația înglobată `expirables!inner`, așa cum semnalează chiar autorul — un șir de forma `x,organization_id.not.is.null` devine injecție de filtru PostgREST și lărgește setul de rânduri returnat.
  CORECTIE: Validează ambii parametri cu `^[a-z][a-z0-9_]{1,48}$` (schemă Zod) înainte de a-i pasa, ignoră valorile neconforme, și construiește interogarea exclusiv cu `.eq()` (valorile sunt codificate de supabase-js); dacă filtrarea pe relație înglobată nu merge, fă două interogări și îmbină în TS.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/lib/email/templates/alerte-expirare.ts
  PROBLEMA: Fragmentul livrat scrie `<a href="${url}" ...>` fără escapare, contrar convenției din `src/lib/email/templates/layout.ts:36` (`escapeHtml(safeUrl(href, appUrl))`). În plus `safeUrl` are DOI parametri obligatorii — `safeUrl(candidate, fallback)` — deci apelul cu un singur argument nici nu compilează. Un ghilimel dublu în URL (parametru construit din etichetă sau din id) închide atributul și permite injectarea de atribute în emailul HTML, iar un URL invalid trece nefiltrat pentru că fallback-ul lipsește.
  CORECTIE: `escapeHtml(safeUrl(url, appUrl))` peste tot, `escapeHtml` pe `numeDestinatar` și pe fiecare `a.eticheta` din `${randuri}`, și refolosește `renderLayout`/`renderButton`/`renderRow` — semnăturile lor sunt exportate din `layout.ts` și acoperă exact acest șablon.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/components/layout/clopotel-notificari.tsx
  PROBLEMA: `listeazaNotificari` are `audit: { action: 'view', entityType: 'notification' }`, iar `createAction` scrie în `audit_logs` la fiecare execuție reușită; clopoțelul o apelează la 60 de secunde, în fiecare filă deschisă. Un utilizator cu trei file produce ~4.300 de rânduri pe zi într-o tabelă append-only (`internal.guard_audit_logs` interzice ștergerea), iar un client rău-intenționat poate apela acțiunea în buclă pentru a umple stocarea firmei și a îneca jurnalul real de conformitate. Nu există `consumeRateLimit` pe această cale.
  CORECTIE: Scoate auditul de pe citirea propriei căsuțe (auditul are sens pe deschiderea unei alerte, nu pe polling), mărește intervalul și oprește polling-ul când fila nu e vizibilă (`document.visibilityState`), și adaugă `consumeRateLimit` pentru acțiunile de listare.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0006_expirables.sql
  PROBLEMA: Coliziune de numerotare: în repo există deja `0006_fix_hr_sensitive.sql` și `0007_fix_hr_rls.sql` (premisa din sarcină, „migrările existente sunt 0001..0005”, este depășită). Un fișier nou numit `0006_expirables.sql` duplică o versiune deja aplicată; `supabase db push` deduplică după prefixul de versiune, deci migrarea nouă riscă să fie considerată aplicată și sărită în tăcere — tabelele și politicile lipsesc din producție, iar codul care le referă eșuează cu 42P01, fără nicio eroare la deploy.
  CORECTIE: Redenumește în `0008_expirables.sql` și verifică cu `supabase migration list` înainte de push.

## produs
VERDICT: Faza 4 NU poate fi declarată gata. Motorul din 0006 e bine gândit ca schemă, dar produsul livrat către un administrator de firmă este, în starea asta, un ecran gol: nimic nu populează `expirables` (niciun trigger pe tabelele HR existente, jobul zilnic e doar pseudocod într-un comentariu), deci semaforul nu se aprinde niciodată și emailul nu pleacă niciodată. Al doilea blocaj de aceeași greutate: căsuța de notificări e închisă cu `compliance:read`, așa că majoritatea angajaților primesc o eroare roșie permanentă în header în loc de propriile notificări. Peste ele, dashboardul numără scadențe dar linkează către alerte — cifrele nu vor coincide niciodată, iar administratorul își pierde încrederea în semafor din prima săptămână. La nivel de limbă, migrarea folosește diacritice cu sedilă (ş/ţ) în chiar mesajele afișate omului, inconsistent cu tot restul aplicației. Ce am putut confirma ca bun: pragurile 30/14/7 SUNT configurabile din interfață (nu doar din bază), idempotența alertelor stă în constrângere nu în cod, iar RLS-ul respectă scope-ul. Minimul pentru „gata”: (1) un modul sursă conectat real + cron activ, (2) inbox-ul de notificări deschis tuturor, (3) o singură sursă de adevăr între dashboard și listă, (4) intrări în navigație, (5) etichete în română în locul codurilor entity_type/kind, (6) email grupat pe urgență cu plafon, (7) corectarea diacriticelor.
- [CRITIC] 0006_expirables.sql (secțiunea 14) + migrările HR
  PROBLEMA: Nimic nu populează `expirables`. Nicio migrare din livrare nu atașează un trigger care să apeleze `internal.sync_expirable()` pe tabelele HR care CHIAR există azi (employment_contracts, work_permits, employee_documents, employee_tax_exemptions), iar jobul zilnic (pg_cron + Edge) rămâne pseudocod într-un comentariu. Scenariu real: firma are 60 de contracte și 12 permise de muncă în bază; administratorul deschide /conformitate și vede „Niciun document urmărit încă”, azi, mâine și peste o lună. Semaforul nu se aprinde niciodată, alertele nu se generează niciodată, emailul nu pleacă niciodată. Din perspectiva clientului, Faza 4 este un ecran gol cu un titlu frumos.
  CORECTIE: Migrare 0007 cu funcții de trigger AFTER INSERT/UPDATE pe cele 3-4 tabele HR existente care apelează sync_expirable, plus backfill al rândurilor curente sub service_role, plus `cron.schedule` real pentru `internal.genereaza_alerte()` / `internal.curata_alerte()`. Până la conectarea unui modul, ecranul trebuie să spună onest „Modulul care generează scadențe nu este încă activat”, nu „nu ați introdus încă date”.
- [CRITIC] src/app/(app)/notificari/actions.ts
  PROBLEMA: Propria căsuță de notificări e protejată cu `permission: 'compliance:read'`. Un angajat obișnuit (rol de portal, magazioner, șofer) nu are această permisiune. Scenariu: omul se autentifică, clopoțelul din header face poll la 60 de secunde, `listeazaNotificari` e refuzat, iar în dropdown apare permanent un mesaj roșu de eroare; /notificari îi arată AccesRestrictionat pentru propriile lui notificări. Efectul: sistemul de notificări e inutilizabil pentru majoritatea utilizatorilor firmei, exact oamenii cărora le expiră permisul sau instruirea.
  CORECTIE: Căsuța proprie nu se protejează cu o permisiune de modul — apartenența la organizație plus filtrul `user_id = ctx.user.id` este autorizarea. Folosește o acțiune fără gating de resursă (sau adaugă resursa `notifications` în grilă și seed-o pentru toate rolurile), și nu afișa niciodată o eroare de permisiune în header.
- [MARE] src/app/(app)/conformitate/page.tsx
  PROBLEMA: Dashboardul numără din `expirables` (scadențe), dar linkurile duc într-o listă care afișează `compliance_alerts` (alerte). Cele două nu pot coincide. Scenariu 1: cardul roșu arată „Expirate 5”, adminul dă click și primește „Nicio alertă pentru filtrele alese”, pentru că regula are `alerteaza_la_depasire=false` sau jobul nu a rulat încă. Scenariu 2: adminul apasă „Am văzut” pe 3 alerte — ele trec în `confirmat`, dispar din filtrul `status=nou`, dar dashboardul continuă să afișeze „Expiră curând 12”. Administratorul concluzionează că aplicația minte și nu mai are încredere în semafor.
  CORECTIE: O singură sursă de adevăr pentru cifre și listă: lista trebuie să fie o listă de SCADENȚE (expirables, cu starea alertei ca atribut), nu o listă de alerte. Linkurile din dashboard să filtreze pe interval de scadență (expirate / ≤N zile), nu pe `status` de alertă.
- [MARE] 0006_expirables.sql
  PROBLEMA: Toate mesajele `raise exception` și notele automate scrise în baza de date folosesc diacritice cu SEDILĂ (ş U+015F, ţ U+0163), nu cu virgulă: „Scadenţa se modifică din modulul care o generează”, „Regula de alertare are nevoie de cel puţin un prag”, „Închisă automat: scadenţa a fost eliminată din documentul sursă”. Restul aplicației (toate .tsx) folosește corect ș/ț cu virgulă. Scenariu: adminul primește un mesaj de eroare în care jumătate din litere se randează cu alt font fallback — arată ca text corupt; iar căutarea în jurnal după „scadența” nu găsește niciodată notele generate automat, pentru că sunt scrise cu alt caracter.
  CORECTIE: Înlocuiește ş→ș (U+0219) și ţ→ț (U+021B) în toate mesajele P0001, în notele automate din `sync_expirable`/`curata_alerte` și în comentarii. Adaugă în CI un grep care respinge U+015F/U+0163/U+015E/U+0162 în migrări și în src/.
- [MARE] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: După „Am văzut” sau „Rezolvă”, acțiunea face `redirect('/conformitate/alerte')` fix, fără parametri. Scenariu: adminul filtrează pe categoria „vehicul”, stare „expirat”, ajunge pe pagina 3 prin cursor, confirmă o alertă — și este aruncat înapoi în lista completă, nefiltrată, de la pagina 1. Cu 40 de alerte de procesat, asta înseamnă 40 de reîncărcări complete și refiltrări manuale. Nu există nici selecție multiplă, nici „confirmă tot ce e pe ecran”.
  CORECTIE: Redirect înapoi la URL-ul curent (păstrează entityType/kind/status/cursor), sau mai bine: buton client cu `router.refresh()` și feedback inline, plus o acțiune în masă pe alertele selectate.
- [MARE] src/app/(app)/conformitate/alerte/actions.ts
  PROBLEMA: `confirmaAlerta` și `rezolvaAlerta` setează doar `status` și `nota`. Coloanele `acknowledged_by` și `resolved_by` rămân NULL pentru totdeauna (triggerul completează doar datele, nu autorul). Scenariu: patronul întreabă „cine a zis că s-a rezolvat ITP-ul la mașina asta?” — în ecran nu apare nimeni, în tabelă e NULL, iar răspunsul se poate afla doar scotocind manual în audit_logs. Exact scopul declarat în comentariul secțiunii 13 din migrare („cine a confirmat, cine a rezolvat”) nu este îndeplinit.
  CORECTIE: Setează `acknowledged_by: ctx.user.id` la confirmare și `resolved_by: ctx.user.id` la rezolvare, adaugă o coloană „Preluată de / Rezolvată de” în tabel și afișează nota existentă (acum nota se poate scrie, dar nu se vede niciodată).
- [MARE] src/lib/email/templates/alerte-expirare.ts
  PROBLEMA: Emailul zilnic listează TOATE alertele, fără plafon, fără grupare și fără o ordonare garantată — atât în HTML cât și în varianta text (`...alerte.map(...)`). Scenariu: o firmă cu flotă de 40 de mașini primește dimineața un email cu 40+ rânduri în care „ITP expirat de 3 zile” stă amestecat cu „RCA expiră peste 30 de zile”. Nu există un rezumat în subiect, nici o secțiune „Urgent acum”. Omul se uită o dată, nu găsește urgența, și în două săptămâni filtrează expeditorul în spam — moment în care întreaga Faza 4 devine inutilă.
  CORECTIE: Subiect cu cifre („3 documente expirate, 5 expiră săptămâna asta”), corp grupat în „Expirate” / „În următoarele 7 zile” / „Mai departe”, sortare după zile rămase, plafon de ~10 rânduri afișate plus „și încă N — vezi lista completă”, și o coloană „zile rămase” în locul pragului tehnic.
- [MARE] src/app/(app)/conformitate/reguli/actions.ts
  PROBLEMA: Regulile se pot doar adăuga, niciodată edita sau șterge din interfață, iar indexul unic e pe (org, entity_type, kind, valabil_de_la) cu `valabil_de_la` implicit = azi. Scenariu: adminul tastează din greșeală „300, 14, 7” în loc de „30, 14, 7”, salvează, își dă seama imediat — și nu are cum să repare: reintroducerea aceleiași combinații azi întoarce „Există deja o regulă … Alegeți altă dată”, iar ștergerea nu există în UI. Rămâne cu praguri greșite minimum până mâine.
  CORECTIE: Adaugă acțiune de corectare a versiunii cu `valabil_de_la` = azi (update pe rândul curent, permis de politica de UPDATE) și o acțiune de anulare (soft delete) a unei versiuni viitoare/curente, ambele cu confirmare.
- [MARE] src/app/(app)/conformitate/page.tsx
  PROBLEMA: Fereastra „Expiră curând” e hardcodată la 30 de zile (`obtineDashboardConformitate(..., 30)`), independent de pragurile pe care administratorul le configurează în /conformitate/reguli. Scenariu: firma setează pragul de 60 de zile pentru autorizații ISCIR, pentru că reînnoirea durează două luni. Un document la 45 de zile generează alertă și email — dar în dashboard este numărat la „În regulă”, pe verde. Semaforul contrazice direct alerta primită pe email.
  CORECTIE: Fereastra dashboardului să fie pragul maxim configurat pentru organizație (sau, minim, un selector vizibil „următoarele 30/60/90 de zile” cu valoarea implicită luată din reguli), iar eticheta cardului să reflecte valoarea reală, nu textul fix „(30 zile)”.
- [MARE] src/components/layout/clopotel-notificari.tsx
  PROBLEMA: Clopoțelul apelează `listeazaNotificari` la fiecare 60 de secunde, iar acțiunea are `audit: { action: 'view' }` — deci fiecare poll scrie un rând în audit_logs. Scenariu: 50 de utilizatori, 8 ore pe zi = ~24.000 de rânduri de zgomot pe zi în jurnalul de audit al organizației. Ecranul de jurnal construit în Faza 1 devine ilizibil (paginile se umplu cu „vizualizare notificări”), iar tabela crește nejustificat. Pollingul continuă și când tabul e ascuns.
  CORECTIE: Scoate auditul de pe citirea listei (auditul are sens pe decizii, nu pe polling), înlocuiește pollingul cu o citire la deschiderea dropdown-ului + Realtime sau `visibilitychange` (fără poll pe tab ascuns) și expune doar un contor ieftin de necitite.
- [MARE] src/config/navigation (neatins)
  PROBLEMA: Nicio intrare de navigație nu a fost adăugată pentru /conformitate, /conformitate/alerte, /conformitate/reguli sau /notificari, deși `NAV_ITEMS` și `BadgeSource` există exact pentru asta. Scenariu: administratorul instalează Faza 4, se autentifică și nu găsește nimic nou în meniu; funcționalitatea e accesibilă doar dacă tastează URL-ul din documentație. În plus, fără `BadgeSource` legat de alertele necitite, nu există niciun indiciu vizual în meniu că ceva a expirat.
  CORECTIE: Adaugă elementele de navigație în grupul potrivit, cu gating pe `compliance:read`, plus badge cu numărul de alerte deschise pe „Conformitate” și de notificări necitite pe clopoțel.
- [MARE] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: Vocabularul intern al bazei de date iese direct în fața administratorului: coloana afișează `{alerta.entityType} / {alerta.kind}` (ex. „employee_document / medical”), iar filtrele „Categorie” și „Tip” sunt câmpuri text libere în care omul ar trebui să ghicească identificatori ca `work_permit` sau `itp`. Pe dashboard, `formateazaCategorie` doar pune majusculă („Employee document”). Scenariu: adminul vrea să vadă doar vizele medicale, tastează „medicale” în „Tip”, primește zero rezultate și conchide că nu are nimic de făcut — deși are 8 vize expirate.
  CORECTIE: Dicționar de etichete în română pentru entity_type/kind (ca `etichetaEntitate` din @/lib/audit/etichete), afișat peste tot; filtrele să fie select-uri populate cu valorile distincte existente, etichetate în română.
- [MEDIU] src/app/(app)/conformitate/page.tsx
  PROBLEMA: Starea goală promite module care nu există în Faza 4: „se completează automat pe măsură ce introduceți vehicule, instruiri SSM…”, iar subtitlul paginii anunță „vehicule, SSM, mentenanță”. Scenariu: administratorul citește, caută în meniu modulul Flotă, nu-l găsește, și sună la suport convins că instalarea e incompletă. În plus, textul spune „Adăugați prima înregistrare într-un modul” fără niciun link — nu are unde să apese.
  CORECTIE: Textele să menționeze doar ce e activ pentru organizația respectivă (verificat cu `getEnabledFeatures`), iar starea goală să conțină un buton concret către modulul disponibil (ex. „Adaugă un contract de muncă”).
- [MEDIU] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: Starea goală spune întotdeauna „Nicio alertă pentru filtrele alese … criteriile de filtrare sunt prea stricte”, chiar și când nu s-a aplicat niciun filtru. Scenariu: firma are toate documentele la zi, adminul intră direct în listă și citește că filtrele lui (inexistente) sunt prea stricte — în loc să afle vestea bună, că nu are nimic de rezolvat. Cazul „zero alerte pentru că totul e în regulă” și cazul „zero rezultate pentru filtrul aplicat” arată identic.
  CORECTIE: Două stări distincte: fără filtre → „Nicio scadență în pericol. Toate documentele urmărite sunt în termen.”; cu filtre → mesajul actual, plus un buton „Șterge filtrele”.
- [MEDIU] src/app/(app)/conformitate/page.tsx
  PROBLEMA: Butonul „Reîncearcă” din starea de eroare este un `<Link>` către exact ruta curentă. Navigarea către URL-ul identic e servită din cache-ul de router și, în cazul fericit, doar re-randează aceeași eroare; nu forțează o nouă interogare. Scenariu: baza de date a avut o pauză de 2 secunde, adminul apasă „Reîncearcă” de trei ori, nu se întâmplă nimic vizibil, iar el crede că aplicația e căzută. Același tipar e copiat în /conformitate/alerte (unde pierde și filtrele) și în /conformitate/reguli. Lipsesc și fișierele loading.tsx pentru alerte și reguli — cele două pagini nu au deloc stare de încărcare.
  CORECTIE: Componentă client mică cu `router.refresh()` (sau `error.tsx` cu `reset()`), plus `loading.tsx` cu SkeletonTable pentru /conformitate/alerte și /conformitate/reguli.
- [MEDIU] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: Probleme de accesibilitate cumulate, toate reproductibile: (1) câmpurile de notă din fiecare rând au doar `placeholder="Notă (opțional)"`, fără label — un utilizator cu cititor de ecran aude „editare text” de 80 de ori, iar la focus placeholderul dispare; (2) în clopoțel, `role="menu"` are copii `<li>`/`<button>` fără `role="menuitem"`, nu se închide cu Escape și focusul nu se întoarce pe buton; (3) starea „necitit” din dropdown e semnalată DOAR prin fundal albastru deschis — invizibilă pentru daltonism/contrast redus; (4) `text-gray-400` pentru dată în /notificari nu trece 4.5:1 pe alb; (5) tab-urile Toate/Necitite sunt link-uri cu `role="tab"` fără `tabpanel`; (6) numărul de necitite se schimbă la 60s fără `aria-live`.
  CORECTIE: Label vizibil sau `sr-only` pe fiecare input de notă (id unic per rând); dropdown-ul fie cu semantică completă de meniu și handler pe Escape + returnarea focusului, fie fără `role="menu"`; adaugă un marcaj neutru la culoare pentru „necitit” (punct + text „Necitită”); ridică textul la `text-gray-600`; scoate `role="tab"` de pe link-uri; `aria-live="polite"` pe contor.
- [MEDIU] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: Lipsesc exact coloanele de care are nevoie cineva care trebuie să acționeze: nu se vede RESPONSABILUL (deși `expirables.responsible_employee_id` există și RLS-ul filtrează după el) și nu se văd ZILELE RĂMASE — doar data scadenței și un „prag” tehnic („30 zile” înseamnă pragul regulii, nu timpul rămas, și se citește exact invers). Scenariu: adminul vede „Scadență 12.09.2026, Prag 30 zile” și trebuie să calculeze mental; și nu știe pe cine să sune. În plus, cifra din coloana „În regulă” de pe dashboard e o fundătură: nu există niciun ecran care să listeze scadențele care NU au alertă, deci „arată-mi toate ITP-urile cu datele lor” e imposibil. Paginarea are doar „Pagina următoare”, fără întoarcere și fără total.
  CORECTIE: Adaugă coloanele „Responsabil” și „Zile rămase” (cu „depășit cu N zile” pe roșu, formulat în cuvinte, nu doar colorat), fă cifra „În regulă” clicabilă către o listă de scadențe, și completează paginarea cu pagina anterioară și un total.
- [MEDIU] src/app/(app)/conformitate/alerte/page.tsx
  PROBLEMA: Mesajul de eroare este transportat prin query string (`?eroare=…`) și randat ca atare într-un bloc `role="alert"`. Orice link trimis pe email către /conformitate/alerte?eroare=… afișează administratorului un text arbitrar care arată exact ca un mesaj oficial al aplicației („Contul organizației a fost suspendat, sunați la 07…”). Separat: subsolul emailului trimite utilizatorul la „Setări > Conformitate” pentru praguri, dar ecranul real este /conformitate/reguli și nu există niciun meniu „Setări > Conformitate”; iar tabela `notification_preferences` (existentă din Faza 1) nu este folosită nicăieri — destinatarul nu are cum să oprească sau să rărească emailurile.
  CORECTIE: Nu transporta textul erorii prin URL: coduri de eroare mapate la mesaje din server, sau `useActionState` cu mesajul păstrat în memorie. Corectează trimiterea din email la calea reală (link direct). Leagă trimiterea emailurilor de `notification_preferences` (zilnic/săptămânal/oprit) și pune linkul de preferințe în subsol.