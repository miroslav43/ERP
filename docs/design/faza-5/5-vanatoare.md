## corectitudine
VERDICT: Modulul are o arhitectură solidă (constrângerea EXCLUDE pentru dubla alocare este corectă — inclusiv semantica `[)` la returnare-repredare în aceeași secundă și limita superioară infinită la `returnat_la IS NULL`; numărul de inventar per organizație este corect izolat între firme, iar `document_sequences` cu INSERT…ON CONFLICT DO UPDATE este atomic). Nu poate fi însă livrat în forma actuală: trei defecte critice îl blochează. (1) Blocul de privilegii de la finalul migrării rulează după COMMIT și modifică drepturile pe TOATE tabelele din schema public — rupe fluxul public de cereri demo și deschide INSERT/UPDATE pentru `authenticated` pe tabele de infrastructură (audit_logs, document_sequences, role_permissions). (2) `app.poate_vedea_expirabil` este rescrisă integral dintr-o listă reconstruită din memorie; tipurile înregistrate de migrarea 0009 dispar tăcut din motorul de expirări. (3) Verificarea de permisiune din pagini (`if (!scopeFor(...))`) nu refuză scope-ul 'none', încălcând S3 în tot modulul. Peste acestea, trei probleme de flux merită oprite înainte de merge: revocarea lotului cade pe eroare FK brută dacă există o alocare ștearsă logic; garanția se proiectează în `expirables` și pentru obiectele fără dată de garanție; iar importul întoarce rândurile validate prin browser și le acceptă înapoi la aplicare, ceea ce anulează validarea server-side și permite duplicarea la re-încercare. Răspunsurile directe la întrebările lentilei: EXCLUDE — corect; număr de inventar reintrodus după greșeală — NU funcționează pentru obiectele introduse manual (rămâne blocat definitiv, contrar promisiunii din antet); același număr în două organizații — permis corect; import — presupune o semnătură neverificată a `src/lib/import/excel.ts`, deci garda pe conținutul decomprimat poate lipsi în practică; revocare după alocare — blocată corect pe cazul principal, dar fisurată pe alocările șterse logic; sincronizarea garanției — cheia include `kind='garantie'`, iar populare inițială nu e necesară (tabelă nouă), dar lipsește garda pe dată NULL și curățarea proiecțiilor la ștergerea fizică.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Blocul de privilegii rulează DUPĂ `commit;` și operează pe TOATE tabelele din schema public: `revoke all on all tables in schema public from anon` + `grant select, insert, update on all tables in schema public to authenticated`. Scenariu 1: formularul public de cerere demo (createPublicAction → demo_requests) rulează ca `anon`; după migrare primește „permission denied for table demo_requests" — modulul de inventar oprește lead-urile site-ului. Scenariu 2: `authenticated` primește INSERT/UPDATE pe audit_logs, document_sequences, role_permissions, organization_features, expirables, invitations — tabele care până acum erau doar-citire la nivel de GRANT; singura apărare rămasă e RLS, iar unde lipsește o politică restrictivă (ex. document_sequences, atins de app.aloca_numar_inventar) apare o cale de scriere directă. Scenariu 3: fiind după COMMIT, dacă unul dintre `revoke`-uri eșuează, tabelele rămân create fără privilegii, iar migrarea e „aplicată" pe jumătate, fără rollback. Cele două revoke-uri compensatorii (rate_limits, employee_sensitive_data) acoperă doar două dintre zecile de tabele afectate.
  CORECTIE: Mută privilegiile ÎNAINTE de `commit;` și enumeră explicit doar tabelele noi: `grant select, insert, update on public.inventory_categories, public.inventory_items, public.inventory_allocations, public.inventory_import_batches to authenticated;` plus `revoke delete on <aceleași 4> from anon, authenticated;`. Elimină complet orice `on all tables in schema public`.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Secțiunea 7 face `create or replace function app.poate_vedea_expirabil` cu o listă de entity_type reconstruită din memorie. Migrarea 0009 (flotă) există în lanț (numerotarea sare de la 0008 la 0010) și aproape sigur a înregistrat tipuri proprii (ex. 'rca', 'itp', 'revizie', 'trip_sheet'). Scenariu: după aplicarea lui 0010, acele tipuri cad pe ramura `else` (`employees:read = 'all'`) și dispar din ecranele de flotă pentru toți utilizatorii care nu au drept total pe personal — o regresie tăcută, fără nicio eroare, într-un modul deja livrat. În plus, `create or replace` cu parametrii denumiți `p_organization_id`/`p_entity_type` eșuează cu „cannot change name of input parameter" dacă funcția reală folosește alte nume, blocând întreaga migrare.
  CORECTIE: Citește corpul curent al funcției (`\sf app.poate_vedea_expirabil` sau `pg_get_functiondef`) și rescrie-l integral, adăugând doar linia 'inventory_item'; păstrează exact numele parametrilor existenți. Pe termen lung, înlocuiește `case` cu un nomenclator `public.expirable_types(entity_type, resource, min_scope)` interogat de funcție, ca fiecare fază să adauge un rând, nu să rescrie funcția.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/angajati/[id]/bunuri/page.tsx
  PROBLEMA: Verificarea de afișare este `if (!scopeFor(permisiuni, 'inventory:read'))`. `scopeFor` întoarce `PermissionScope | null`, iar 'none' este o valoare de scope validă și TRUTHY. Scenariu: administratorul organizației setează explicit rolului „angajat" scope 'none' pe inventory:read ca să interzică modulul; `scopeFor` întoarce 'none', condiția e falsă, pagina se randează integral și execută interogările. Datele sunt oprite abia de RLS (dacă app.can tratează 'none'), deci utilizatorul vede un ecran fals-gol în loc de refuz — iar S3 („scope 'none' e REFUZ EXPLICIT") e încălcat. Același tipar apare în toate paginile modulului (inventar/page.tsx, inventar/[id], inventar/alocari, inventar/import).
  CORECTIE: Folosește `can(permisiuni, 'inventory:read', 'own')` sau `meetsScope(scopeFor(permisiuni, 'inventory:read') ?? undefined, 'own')` și tratează explicit rezultatul fals cu `<AccesRestrictionat mesaj="…" />`. Aplică peste tot în modul.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `app.revoca_import_inventar` verifică existența alocărilor cu `and a.deleted_at is null`, dar FK-ul `inventory_allocations.item_id references public.inventory_items(id) on delete restrict` NU ține cont de soft delete. Scenariu concret: un obiect din lot a fost predat, predarea s-a dovedit greșită și a fost anulată (deleted_at setat), apoi magazionerul revocă lotul. Garda de business trece (v_alocate = 0), dar `delete from public.inventory_items where import_batch_id = ...` cade pe 23503 cu textul brut „update or delete on table "inventory_items" violates foreign key constraint" — mesaj de programator, exact ce interzice regula de limbă, iar lotul rămâne în starea 'in_lucru'/'finalizat'. În plus, garda contrazice motivația din antet („după prima predare-primire există istoric juridic"): o predare anulată logic ESTE istoric.
  CORECTIE: Scoate `and a.deleted_at is null` din subinterogarea `exists` (orice alocare, ștearsă logic sau nu, blochează revocarea). Alternativ, dacă se dorește permiterea revocării, șterge întâi fizic alocările soft-deleted ale lotului în aceeași funcție, înainte de `delete from public.inventory_items`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `internal.inventory_sync_garantie` apelează necondiționat `internal.sync_expirable(...)` cu `v_obiect.garantie_expira` și `is_active = v_obiect.status <> 'casat'`. Marea majoritate a obiectelor de inventar (mobilier, scule vechi, obiecte importate fără dată de garanție) au `garantie_expira` NULL. Scenariu: un import de 2000 de obiecte fără garanție generează 2000 de proiecții în `expirables` cu dată scadentă NULL și is_active = true. Fie inserarea cade pe NOT NULL în `expirables` și importul eșuează integral, fie motorul de alerte din Faza 4 primește 2000 de scadențe fără dată și calculează praguri pe NULL. Nicăieri în trigger nu există garda `if new.garantie_expira is null`.
  CORECTIE: Calculează activarea din dată, nu doar din status: `v_obiect.garantie_expira is not null and v_obiect.status <> 'casat'`, sau ieși devreme din funcție (`if v_obiect.garantie_expira is null then perform internal.sync_expirable(..., is_active => false); return; end if;`) — după ce verifici în 0008_expirables.sql cum tratează sync_expirable o dată NULL.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Ștergerea fizică a obiectelor lasă în urmă rânduri în `expirables`. În `app.revoca_import_inventar` se apelează `sync_expirable(..., null::date, ..., false)` înainte de DELETE — dacă funcția din 0008 doar dezactivează (is_active=false) și nu șterge rândul, în `expirables` rămân rânduri cu `entity_type='inventory_item'` și `entity_id` care nu mai există în nicio tabelă. Scenariu agravant: se importă din nou același fișier, se generează alte UUID-uri, iar `expirables` acumulează la fiecare revocare-reimport. Același defect, mai grosolan, în seed: `supabase/seed-inventar.sql` face `delete from public.inventory_items where numar_inventar like 'DEMO-INV-%'` fără niciun apel de curățare a proiecțiilor, deci fiecare rulare a seed-ului lasă rânduri orfane.
  CORECTIE: După `delete from public.inventory_items ...` adaugă ștergerea explicită a proiecțiilor: `delete from public.expirables where organization_id = v_org and entity_type = 'inventory_item' and entity_id = any(v_ids);` (colectează id-urile într-un array înainte de DELETE, prin `returning id`). Adaugă aceeași curățare în seed.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: Rândurile validate fac drum-întors prin browser: `pregatesteImportInventar` le întoarce clientului (`setValide`), iar `aplicaImportInventar({ batchId, randuri: valide })` le trimite înapoi ca payload. Scenariu: un utilizator cu drept de import modifică payload-ul cererii (sau un script) și inserează obiecte cu valori/numere de inventar care nu au existat în fișier; validarea din etapa „pregătește" nu mai are nicio legătură cu ce se scrie efectiv. Al doilea scenariu, fără rea-credință: `aplica()` eșuează la jumătatea inserărilor, `etapa` rămâne 'previzualizare', utilizatorul apasă din nou butonul și retrimite ÎNTREG setul `valide` — obiectele deja inserate produc 23505 pe indexul unic, iar cele reușite consumă a doua oară numere din `document_sequences`. Nu există nicio idempotență pe `batchId`.
  CORECTIE: Nu trimite datele înapoi de la client: fie persistă rândurile validate server-side (tabelă de staging legată de `import_batch_id`), fie re-parsează fișierul stocat în etapa de aplicare, iar clientul trimite doar `batchId`. Fă `aplicaImportInventar` idempotentă: refuză un lot al cărui `status` nu mai e 'in_lucru' și marchează statusul în aceeași tranzacție cu inserările.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Migrarea depinde de helperi SQL care nu apar în inventarul confirmat de funcții: `app.azi_local()` (folosit în internal.inventory_items_valideaza, în app.aloca_numar_inventar și de 8 ori în seed), `internal.seteaza_updated_at()`, `internal.set_actor()`, precum și arietatea presupusă a lui `app.write_audit('delete', org, entity_type, entity_id, null, jsonb)`. Scenariu: `app.azi_local` nu există sau are alt nume (ex. `app.today_local`, sau se folosea `(now() at time zone 'Europe/Bucharest')::date`) → migrarea cade la primul `create function` cu „function app.azi_local() does not exist"; dacă `app.write_audit` are altă ordine/arietate a parametrilor, eroarea apare abia la prima revocare de lot, în producție.
  CORECTIE: Verifică fiecare nume înainte de aplicare: `select p.oid::regprocedure from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname in ('app','internal') and p.proname in ('azi_local','write_audit','can','seteaza_updated_at','set_actor','sync_expirable');` și aliniază apelurile la semnăturile reale din 0002/0004/0008.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Antetul promite o cale de corecție („Un bun greșit introdus se ȘTERGE fizic"), dar ea există DOAR pentru obiectele venite dintr-un lot de import. Scenariu real: magazionerul adaugă manual, din formular, obiectul „DELL-7420", apoi constată că e un duplicat al unui obiect deja înregistrat. Nu îl poate șterge: DELETE e revocat de la authenticated, nu există politică DELETE, `import_batch_id` e NULL deci `app.revoca_import_inventar` nu îl atinge, iar tabela nu are `deleted_at`. Singura opțiune e `status='casat'`, care păstrează rândul — deci numărul de inventar rămâne consumat pentru totdeauna pe indexul unic NEFILTRAT, exact scenariul (b) pe care antetul îl declară inacceptabil. Chiar și corectarea prin UPDATE nu ajută dacă numărul corect e deja luat de duplicatul real.
  CORECTIE: Adaugă o funcție simetrică `app.sterge_obiect_inventar(p_item_id uuid, p_motiv text)`, SECURITY DEFINER, care refuză explicit dacă există orice rând în inventory_allocations pentru obiect (cu sau fără deleted_at), curăță proiecțiile din expirables, scrie audit și șterge fizic rândul — aceleași precondiții ca la revocarea de lot, dar la nivel de obiect.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Două căi de scriere depind tăcut de faptul că proprietarul funcțiilor SECURITY DEFINER are BYPASSRLS, deși toate tabelele au FORCE ROW LEVEL SECURITY (care se aplică inclusiv proprietarului). (a) `app.revoca_import_inventar` face DELETE pe o tabelă fără politică DELETE. (b) `internal.inventory_alloc_propaga` face UPDATE pe inventory_items când un ANGAJAT își confirmă primirea (UPDATE pe inventory_allocations declanșează triggerul AFTER); angajatul nu are `inventory:update` la scope 'all', deci sub FORCE RLS fără BYPASSRLS clauza USING a politicii `inventory_items_update` filtrează rândul și UPDATE-ul actualizează 0 rânduri — TĂCUT, fără eroare: `stare` și `status` obiectului rămân nesincronizate, iar defectul nu se vede decât în raportul de gestiune.
  CORECTIE: Verifică `select rolbypassrls from pg_roles where rolname = current_user;` pe instanța țintă. Independent de rezultat, adaugă o politică de scriere pentru contextul de serviciu (`app.is_service_context()`) sau ridică în propaga un `set local role` controlat; iar pentru (b) mută propagarea într-o funcție care rulează cu drepturile proprietarului garantat exempt și verifică `get diagnostics row_count` — dacă e 0, ridică P0001 în loc să continue tăcut.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: În `internal.inventory_alloc_propaga`, `stare = coalesce(new.stare_la_returnare, i.stare)` se aplică la ORICE update al unei alocări, inclusiv al uneia istorice. Scenariu: laptopul a fost returnat în martie ca 'uzat', repredat în aprilie și returnat în iunie ca 'defect' (obiectul e acum 'defect'). În iulie administratorul corectează o observație pe alocarea din martie și schimbă `stare_la_returnare` din 'uzat' în 'bun'; triggerul suprascrie starea CURENTĂ a obiectului cu 'bun', deși obiectul e defect. Fișa obiectului minte, iar cauza nu apare nicăieri în UI.
  CORECTIE: Aplică propagarea stării doar pentru alocarea deschisă/tocmai închisă: adaugă condiția `and new.returnat_la is not null and not exists (alocare deschisă mai nouă)` sau, mai simplu, actualizează `stare` doar când `new.returnat_la is distinct from old.returnat_la` (tranziția reală de returnare), nu la orice UPDATE.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Politica `inventory_items_select` filtrează alocările doar cu `a.deleted_at is null`, fără `a.returnat_la is null`, deși comentariul de deasupra spune „'own' = ce am eu în primire". Scenariu: un angajat care în 2024 a avut în primire un laptop și l-a returnat continuă să vadă la nesfârșit fișa LIVE a acelui obiect — valoare de inventar, locație curentă, status — inclusiv după ce obiectul a ajuns la alt coleg sau în altă locație. La scope 'team' e mai grav: managerul vede fișele tuturor obiectelor pe care echipa lui le-a atins vreodată.
  CORECTIE: Decide explicit politica: dacă „own" înseamnă deținere curentă, adaugă `and a.returnat_la is null` în ambele ramuri EXISTS; dacă se dorește acces la istoric, restrânge coloanele expuse printr-o vedere dedicată (fără valoare/locație) în loc să deschizi rândul întreg.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/alocari/actions.ts
  PROBLEMA: Constrângerea `inventory_alloc_fara_suprapunere` este calea normală de eșec la dubla predare și la predările retroactive, dar nu există nicăieri o traducere a SQLSTATE 23P01 (exclusion_violation). Scenariu: doi magazioneri predau simultan același laptop; al doilea primește, prin `mapPostgrestError`, mesajul brut „conflicting key value violates exclusion constraint \"inventory_alloc_fara_suprapunere\"" — text de programator, în engleză, exact ce interzice regula de limbă a proiectului. Același mesaj apare la orice predare retroactivă care se suprapune cu o deținere anterioară, adică la o eroare de operare complet obișnuită.
  CORECTIE: Adaugă în `@/lib/actions/errors` (sau în handler-ul acțiunii de predare) maparea explicită 23P01 → businessRule('Obiectul este deja predat altui angajat în perioada indicată. Înregistrați mai întâi returnarea.') și 23505 pe `inventory_items_numar_uq` → 'Există deja un obiect cu acest număr de inventar.'
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/angajati/[id]/bunuri/page.tsx
  PROBLEMA: `const { data: itemi } = idItemi.length > 0 ? await supabase.from('inventory_items').select(...) : { data: [] as { id: string; denumire: string; numar_inventar: string; status: string; stare: string }[] };` produce un tip UNIUNE între rezultatul tipat al PostgREST (unde `status`/`stare` sunt enum-urile din Database) și array-ul literal cu `string`. Sub `strict`, apelul `(itemi ?? []).map(...)` pe o uniune de tipuri de array eșuează cu „This expression is not callable — signatures of members are not compatible", iar `status: string` nu e atribuibil enum-ului. Pagina nu compilează.
  CORECTIE: Evită ternarul pe rezultatul destructurat: declară `let itemi: Tables<'inventory_items'>[] = []` (sau tipul select-ului) și populează-l într-un `if (idItemi.length > 0) { const { data } = await ...; itemi = data ?? []; }`. Același tipar trebuie verificat oriunde s-a folosit fallback-ul `{ data: [] as ... }`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: `descarcaRaportRespinse` creează un `<a>` care NU e adăugat în document și revocă imediat obiectul URL după `click()`. În Firefox, click pe un anchor nedetașat nu declanșează descărcarea, iar `URL.revokeObjectURL` sincron poate invalida blob-ul înainte ca descărcarea să pornească — magazionerul apasă „Descarcă raportul rândurilor respinse" și nu se întâmplă nimic, fără niciun mesaj. Suplimentar, escaparea CSV `replace(/"/g, "''")` este greșită: în CSV ghilimeaua se dublează (`""`), nu se înlocuiește cu două apostrofuri, iar mesajele de eroare care conțin ghilimele ajung deformate în raport.
  CORECTIE: `document.body.appendChild(link); link.click(); link.remove(); setTimeout(() => URL.revokeObjectURL(url), 0);` și schimbă escaparea în `.replace(/"/g, '""')`. Adaugă BOM UTF-8 (`﻿`) în Blob, altfel diacriticele apar corupte la deschiderea în Excel românesc.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/actions.ts
  PROBLEMA: Fișierul .xlsx este trimis ca `File` direct într-o Server Action. Limita implicită de corp pentru Server Actions în Next este 1 MB: scenariu — magazionerul încarcă inventarul firmei (câteva mii de rânduri, ~2 MB), primește o eroare generică de rețea, nu un mesaj de business, și nu are cum să înțeleagă ce s-a întâmplat. Separat, semnătura `citesteExcel(fisier, { randuriMaxime })` este PRESUPUSĂ; dacă modulul real `src/lib/import/excel.ts` expune garda pe dimensiunea decomprimată printr-un alt parametru (sau printr-o funcție diferită), importul de inventar rulează fără protecție anti-zip-bomb, spre deosebire de importul de angajați care o are.
  CORECTIE: Configurează `serverActions.bodySizeLimit` pentru ruta de import și validează dimensiunea în client înainte de trimitere, cu mesaj în română. Citește `src/lib/import/excel.ts` și apelează exact aceeași funcție cu aceiași parametri de limită ca importul de angajați — nicio copie locală a logicii de parsare.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `internal.inventory_items_valideaza` fixează pe UPDATE doar `organization_id` și `created_at`, dar lasă `import_batch_id` liber modificabil. Scenariu: un utilizator cu `inventory:update`/'all' setează `import_batch_id` al unui obiect vechi, introdus manual și nealocat vreodată, la id-ul unui lot recent, apoi apelează `app.revoca_import_inventar` pe acel lot — obiectul, care nu a venit din import, este ȘTERS FIZIC împreună cu lotul, fără urmă individuală în audit (auditul consemnează doar numărul agregat de obiecte șterse). Este singura cale de ștergere fizică din modul și poate fi redirecționată către orice obiect nealocat.
  CORECTIE: Adaugă în trigger, pe ramura UPDATE, `new.import_batch_id := old.import_batch_id;` (provenienței nu i se schimbă istoria). Dacă e nevoie de re-legare, fă-o printr-o funcție dedicată care scrie audit per obiect.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Ghilimelele românești sunt greșite peste tot în migrare: se deschide corect cu „ (U+201E) dar se închide cu ghilimea dreaptă " în loc de ” (U+201D) — de ex. mesajul de eroare afișat administratorului: 'Obiectul nu poate fi marcat „alocat" fără o predare-primire înregistrată.', comentariul constrângerii de excludere ('Un obiect, un deținător…' e corect, dar „[)" nu), comentariul funcției de numerotare ('… („INV-2026-00042")'), și comentariile de antet („inv 12" / „INV 12"). În SQL nu sparge fișierul, dar mesajul ajunge ca atare în interfață, la administratorul firmei.
  CORECTIE: Înlocuiește fiecare " de închidere cu ” în toate literalele și comentariile migrării. Verificare rapidă înainte de commit: `grep -n '„[^”]*"' supabase/migrations/0010_inventory.sql`.

## securitate
VERDICT: Modulul are o structură de izolare bună în intenție (scope-uri diferențiate, EXCLUDE pe suprapunerea alocărilor, refuz implicit în motorul de expirări), dar are două găuri care trebuie închise înainte de orice aplicare: (1) blocul final de privilegii acordă `select, insert, update on all tables in schema public` rolului `authenticated` și retrage tot accesul rolului `anon`, adică o migrare de inventar rescrie matricea de acces a întregii baze, în afara tranzacției; (2) `app.revoca_import_inventar` șterge fizic obiecte filtrând doar pe `import_batch_id`, o coloană scriibilă de client și neconstrânsă la organizație — ștergere cross-tenant. La întrebările lentilei: un angajat obișnuit NU vede inventarul firmei prin `inventory_items` (politica e corectă), dar îl poate vedea prin proiecția în `expirables`, înregistrată la pragul 'own' pentru obiecte al căror responsabil e NULL; NU poate confirma în numele altcuiva, în schimb administratorul poate confirma în numele lui, iar câmpul nu reține niciun actor; NU își poate aloca singur un obiect (INSERT-ul cere 'all'). Importul acceptă înapoi de la browser rezultatul propriei validări și nu are nicio limită declarată pe conținutul decomprimat al arhivei .xlsx. Procesul-verbal are un număr derivat dintr-un COUNT filtrat de RLS, deci diferă în funcție de cine îl deschide, iar calea documentului nu are contract de prefix pe organizație.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Blocul de privilegii de la finalul migrării (după `commit;`) face `revoke all on all tables in schema public from anon` și `grant select, insert, update on all tables in schema public to authenticated`. Un modul de inventar re-deschide, cu o singură comandă, TOATE tabelele din public către rolul `authenticated` (audit_logs, role_permissions, organization_features, document_sequences, invitations, platform_admins, email_log, expirables, employment_contracts…). Autorul a observat pericolul și a reînchis doar două tabele (`rate_limits`, `employee_sensitive_data`) — restul rămân deschise. Orice tabelă din fazele anterioare care se baza pe lipsa GRANT-ului (nu doar pe RLS) devine scriibilă: de ex. `document_sequences` cu UPDATE acordat permite, dacă are vreo politică permisivă de membru, rescrierea numerelor de document. Simetric, `revoke all ... from anon` taie accesul anonim pe TOATE tabelele — inclusiv fluxul public (`createPublicAction`, `demo_requests`, branding public), care se rupe silențios la deploy. În plus blocul rulează în AFARA tranzacției: dacă eșuează, migrarea rămâne pe jumătate aplicată, iar `revoke delete on all tables ... from public, anon, authenticated` retrage global DELETE, afectând module care nu au legătură cu inventarul.
  CORECTIE: Înlocuiește complet blocul cu granturi nominale, în interiorul tranzacției: `grant select, insert, update on public.inventory_categories, public.inventory_items, public.inventory_allocations, public.inventory_import_batches to authenticated;` și `revoke all on public.inventory_categories, ... from anon;`. Nicio comandă `on all tables in schema public` într-o migrare de modul.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `app.revoca_import_inventar` verifică dreptul pe organizația LOTULUI (`v_org`), dar ștergerea fizică nu filtrează pe organizație: `delete from public.inventory_items where import_batch_id = p_batch_id;`. `inventory_items.import_batch_id` este o coloană liberă, scrisă de client (politica de INSERT/UPDATE nu o fixează și nicio constrângere nu impune ca lotul referit să aparțină aceleiași organizații). Scenariu: administratorul de inventar al firmei A află/ghicește ID-ul unui lot al firmei B (id de lot apare în URL-uri de import, în rapoarte, în audit) și setează `import_batch_id` al propriilor obiecte pe acel lot. Când administratorul firmei B revocă lotul său — operație legitimă, în organizația sa — funcția șterge FIZIC și obiectele firmei A. Aceeași scăpare există la `perform internal.sync_expirable(v_org, ..., i.id, ...)` care proiectează scadențele obiectelor firmei A sub organization_id-ul firmei B, contaminând tabela `expirables` cross-tenant. `count(*)` de precondiție are aceeași lipsă de filtru.
  CORECTIE: Adaugă `and i.organization_id = v_org` la toate cele trei interogări asupra `public.inventory_items` din funcție și, separat, o constrângere de integritate care refuză `import_batch_id` dintr-o altă organizație (trigger BEFORE pe inventory_items: citește `inventory_import_batches.organization_id` și compară cu `new.organization_id`, errcode P0001).
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/angajati/[id]/bunuri/page.tsx
  PROBLEMA: Poarta de permisiune este `if (!scopeFor(permisiuni, 'inventory:read')) return <AccesRestrictionat .../>`. `scopeFor` întoarce `PermissionScope | null`, iar `PermissionScope` include valoarea `'none'` (S3: „scope 'none' e REFUZ EXPLICIT”). Șirul `'none'` este truthy în JS, deci un utilizator căruia matricea de roluri i-a REFUZAT explicit inventarul trece de poartă și pagina se randează. Rămâne doar RLS ca apărare — exact situația pe care S2 o interzice (permisiunea trebuie verificată și la afișare). Efectul vizibil: utilizatorul refuzat vede antetul cu numele și prenumele angajatului țintă (citit din `employees`, care are alt regim de RLS decât inventarul) plus mesajul „Niciun obiect alocat”, în loc de refuz. Același tipar de verificare apare și în celelalte pagini de inventar livrate.
  CORECTIE: Verifică scope-ul efectiv, nu doar prezența lui: `const scope = scopeFor(permisiuni, 'inventory:read'); if (!meetsScope(scope ?? undefined, 'own')) return <AccesRestrictionat .../>;` (sau `can(permisiuni, 'inventory:read', 'own')`). Aplică-l în toate paginile modulului.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Înregistrarea în `app.poate_vedea_expirabil` este `when 'inventory_item' then app.can(p_organization_id, 'inventory', 'read', 'own')` — prag minim 'own'. Dar politica `inventory_items_select` ascunde deliberat obiectele NEALOCATE de toți cei care nu au scope 'all' („inventarul din magazie nu este treaba fiecărui angajat”). Eticheta scrisă în `expirables` este `'Garanție — ' || denumire || ' (' || numar_inventar || ')'`, iar `responsible_employee_id` este NULL exact pentru obiectele nealocate. Comentariul presupune că politica din 0008 filtrează ea pe `responsible_employee_id` — dar pentru rândurile cu responsabil NULL nu are ce filtra; dacă 0008 le tratează ca vizibile oricui trece de `poate_vedea_expirabil`, orice angajat cu `inventory:read` la 'own' (adică oricine folosește portalul) poate enumera prin `expirables` denumirea și numărul de inventar al ÎNTREGULUI patrimoniu al firmei — un canal de citire paralel, mai slab decât politica pe care modulul o declară.
  CORECTIE: Fie ridică pragul la `app.has_permission(p_organization_id,'inventory','read') = 'all'` pentru `inventory_item`, fie verifică în 0008 că ramura 'own' cere explicit `responsible_employee_id = app.current_employee_id(...)` (deci NULL nu este vizibil) și scrie în etichetă doar numărul de inventar, fără denumire, până la confirmare.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: Etapa de aplicare trimite înapoi la server rândurile validate de etapa anterioară: `aplicaImportInventar({ batchId, randuri: valide })`, unde `valide` este stare React, adică date sub controlul total al clientului. Validarea făcută în `pregatesteImportInventar` (fișier citit server-side) nu are nicio legătură criptografică sau de stocare cu ce se aplică: un utilizator cu drept de import poate apela direct acțiunea cu un `randuri` fabricat (numere de inventar arbitrare, valori arbitrare, câmpuri pe care validatorul de import le respinsese) sau cu `batchId` al unui alt lot, deja finalizat, ca să-i altereze statistica și să-și „ascundă” obiectele într-un lot vechi. Contractul corect este ca serverul să nu accepte niciodată de la client rezultatul propriei validări.
  CORECTIE: Persistă rândurile validate server-side (tabelă de staging legată de batch, sau re-citirea fișierului din storage) și trimite de la client DOAR `batchId`. Dacă round-trip-ul rămâne, re-rulează integral schema Zod de import pe fiecare rând primit în `aplicaImportInventar` și verifică `batchId` — apartenența la `ctx.tenant.organizationId`, `status = 'in_lucru'` și `importat_de = ctx.user.id`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `confirmat_de_angajat_la` este semnătura electronică a angajatului pe predarea-primire (baza răspunderii materiale), dar oricine are `inventory:update` la scope 'all' o poate scrie pentru orice alocare: în `internal.inventory_alloc_valideaza` ramura `v_privilegiat` sare peste toate verificările, iar politica `inventory_allocations_update` permite administratorului să atingă orice rând. Nu există nicio coloană `confirmat_de`, deci după scriere rândul afirmă „angajatul a confirmat primirea” fără niciun actor atașat. Scenariu: magazionerul predă un laptop, angajatul refuză să semneze, magazionerul setează el `confirmat_de_angajat_la` — fișa arată o confirmare care nu a existat.
  CORECTIE: Adaugă `confirmat_de uuid references auth.users(id)` și, în trigger, permite scrierea lui `confirmat_de_angajat_la` NUMAI când `new.employee_id = app.current_employee_id(new.organization_id)` (sau `app.is_service_context()`), forțând `new.confirmat_de := auth.uid()`. Ramura de admin trebuie să fie exclusă explicit din acest câmp, nu inclusă implicit.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Restricționarea pe coloane pentru angajatul neprivilegiat din `internal.inventory_alloc_valideaza` este o LISTĂ NEAGRĂ: enumeră `employee_id, predat_la, returnat_la, stare_la_predare, stare_la_returnare, observatii, pv_document_path, deleted_at`. Lipsesc `id`, `created_at`, `created_by`, `updated_by`, `organization_id` (aceasta e forțată, dar restul nu). Concret: un angajat obișnuit, care are dreptul de UPDATE pe propriul rând, poate rescrie `created_by` — adică poate falsifica cine a înregistrat predarea către el — și poate schimba cheia primară `id` a alocării, rupând orice referință externă (link-ul către procesul-verbal, rândurile de audit). Mai grav pe termen lung: orice coloană adăugată în viitor acestei tabele devine automat scriibilă de către angajat, fără ca cineva să-și amintească să actualizeze lista.
  CORECTIE: Inversează logica într-o listă albă: pentru actorul neprivilegiat, construiește `to_jsonb(new) - 'confirmat_de_angajat_la' - 'updated_at' - 'updated_by'` și compară cu echivalentul lui `old`; orice diferență → P0001. Așa, coloanele noi sunt implicit interzise.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Antetul migrării argumentează că ștergerea fizică este permisă „numai în bloc, pe import_batch_id, ÎNAINTE de orice alocare”. Politica `inventory_items_update` nu protejează însă `import_batch_id`: orice utilizator cu `inventory:update` la 'all' poate seta acest câmp pe obiecte introduse MANUAL, nelegate de vreun import, apoi poate apela `app.revoca_import_inventar` și le șterge fizic. Ștergerea nu trece prin `internal.attach_audit` (care ascultă doar INSERT/UPDATE), iar consemnarea explicită trimite `null` ca payload vechi: `app.write_audit('delete', v_org, 'inventory_items', p_batch_id, null, jsonb_build_object(... 'obiecte_sterse', v_sterse ...))`. Rezultat: obiecte de patrimoniu dispar definitiv, iar jurnalul reține doar un număr, nu și ce s-a șters. Este exact calea de eludare a regulii de soft delete pe care restul sistemului o impune.
  CORECTIE: Fixează `import_batch_id` ca imuabil după insert (în triggerul BEFORE: `new.import_batch_id := old.import_batch_id` la UPDATE) și, în `revoca_import_inventar`, scrie în audit conținutul rândurilor șterse: colectează `jsonb_agg(to_jsonb(i))` înainte de DELETE și trimite-l ca payload vechi, câte o intrare pe obiect sau una agregată.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/actions.ts
  PROBLEMA: Importul primește un `File` direct în server action și îl dă unui parser de Excel. Singura limită vizibilă este `accept=".xlsx,.xls"` pe input-ul din browser, care nu este un control de securitate. Un .xlsx este o arhivă ZIP: un fișier de câteva sute de kiloocteți poate conține un `sharedStrings.xml` de mai mulți gigaocteți după decomprimare (zip bomb clasic), iar o foaie poate declara milioane de rânduri sau un interval de dimensiune arbitrară. Verificarea de mărime — dacă există — se face pe octeții COMPRIMAȚI, nu pe conținutul decomprimat. Un singur utilizator cu drept de import epuizează memoria procesului Node și oprește aplicația pentru toți chiriașii (același proces servește toate organizațiile).
  CORECTIE: Verifică `fisier.size` înainte de parsare (ex. max 5 MB), impune plafoane pe conținutul DECOMPRIMAT în timpul citirii — număr maxim de foi, de rânduri (`randuriMaxime`), de celule și de octeți citiți cumulat, cu abandon imediat la depășire — și validează tipul MIME real, nu extensia. Aplică și `consumeRateLimit` din `@/lib/utils/rate-limit` pe acțiunea de import.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Migrarea își sprijină TOATE porțile de scriere pe `app.can(uuid, text, text, text)` (politicile de INSERT/UPDATE pe cele patru tabele, ramura `v_privilegiat` din trigger, gardele din `app.aloca_numar_inventar` și `app.revoca_import_inventar`) și pe `app.azi_local()`. Niciuna dintre cele două nu figurează în inventarul de helperi SQL existenți, care documentează doar `app.has_permission(uuid, text, text)` (trei argumente, întoarce scope-ul ca text). Dacă `app.can` nu există, migrarea eșuează la aplicare; dacă există cu altă ordine a argumentelor sau cu altă semantică (de ex. `app.can(org, 'inventory:update', scope)` cu cheie compusă, sau interpretând al patrulea argument ca scope EXACT în loc de prag minim), gardele își schimbă tăcut înțelesul — un `app.can(org,'inventory','update','all')` interpretat ca „scope exact all” refuză administratorii, iar interpretat greșit invers acordă scriere celor cu scope 'own'.
  CORECTIE: Confirmă empiric semnătura în 0002_authz.sql (`\df app.can`). Dacă nu există, exprimă toate gardele prin helperul documentat: `app.has_permission(org,'inventory','update') = 'all'`. Aceeași verificare pentru `app.azi_local()` — altfel folosește `(now() at time zone 'Europe/Bucharest')::date`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Toate politicile de INSERT/UPDATE conțin `created_by = (select auth.uid())` / `updated_by = (select auth.uid())` ca element de S8, dar aceste coloane sunt scrise de triggerul BEFORE `internal.set_actor()`, atașat pe toate cele patru tabele. RLS WITH CHECK se evaluează DUPĂ triggerele BEFORE ROW, deci clauzele sunt tautologic adevărate indiferent ce trimite clientul — nu constituie niciun control, dar creează impresia că atribuirea actorului este verificată la nivel de politică. La `inventory_import_batches` există și un caz de S8 incomplet real: se zerofică `randuri_importate` și `randuri_esuate`, dar `randuri_total` (tot o valoare calculată de server din fișier) poate fi trimisă arbitrar de client la INSERT.
  CORECTIE: Elimină din WITH CHECK condițiile pe coloane scrise de triggere (sau mută atribuirea actorului din trigger în politică, nu ambele) și adaugă `randuri_total = 0` în WITH CHECK-ul de INSERT pe loturi, lăsând serverul să o actualizeze după parsare.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `inventory_batches_update` verifică doar organizația, feature-ul, dreptul și `updated_by`. Nimic nu împiedică rescrierea câmpurilor de atribuire și de stare finală: un utilizator cu `inventory:update` poate seta `importat_de` pe un coleg (astfel încât un import greșit să pară făcut de altcineva), poate scrie `revocat_la`/`revocat_de`/`status = 'revocat'` direct prin PostgREST fără să treacă prin `app.revoca_import_inventar` (deci fără verificarea „niciun obiect predat” și fără urma de audit a ștergerii), sau poate readuce un lot revocat în `in_lucru`.
  CORECTIE: În triggerul BEFORE al loturilor, forțează la UPDATE `new.importat_de := old.importat_de`, `new.importat_la := old.importat_la`, și refuză cu P0001 orice trecere în `revocat` sau scriere a lui `revocat_la`/`revocat_de` în afara contextului funcției (marchează contextul cu un `set_config` local setat de `app.revoca_import_inventar`).
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Nici `category_id`, nici `import_batch_id` din `inventory_items` nu sunt verificate ca aparținând aceleiași organizații (cheile străine sunt simple, fără componentă de tenant, și niciun trigger nu compară). Un utilizator din firma A poate crea un obiect care referă o categorie privată a firmei B: FK-ul `on delete restrict` pe categorii blochează atunci ștergerea/curățarea rândului firmei B dintr-o organizație străină, iar existența ID-ului este confirmată prin succesul/eșecul insertului (oracol de enumerare). Aceeași observație susține și scenariul de ștergere cross-tenant de la `import_batch_id`.
  CORECTIE: Trigger BEFORE pe `inventory_items` care validează: categoria are `organization_id is null` sau `= new.organization_id`; lotul are `organization_id = new.organization_id`. Mesaje P0001 în română.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `pv_document_path` este text liber de până la 500 de caractere, fără niciun contract de cale (nu există CHECK pe prefix, nu există verificare că traseul conține organization_id). Procesul-verbal de predare-primire conține date cu caracter personal (nume, prenume, funcție, semnătură). În momentul în care se adaugă generatorul de URL-uri semnate — inevitabil în faza următoare — el va primi această cale exact așa cum a scris-o utilizatorul; o cale de forma `<uuid-organizație-străină>/pv/...` produce un URL semnat valid către documentul altui chiriaș, fiindcă semnarea în Supabase Storage nu re-verifică apartenența.
  CORECTIE: Impune contractul acum, cât timp coloana e goală: `constraint inventory_alloc_pv_prefix check (pv_document_path is null or pv_document_path like organization_id::text || '/%')` și documentează bucket-ul așteptat în comentariul coloanei, ca generatorul viitor să nu poată devia.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/lib/documents/proces-verbal.ts
  PROBLEMA: Numărul procesului-verbal este calculat, potrivit propriei semnalări a livrării, ca poziția alocării în anul predării, per organizație — adică dintr-un COUNT peste `inventory_allocations`. Interogarea trece prin RLS: la scope 'all' numără toate alocările, la scope 'own' numai pe ale utilizatorului. Același document, deschis de magazioner și de angajat, primește DOUĂ numere diferite. În plus calculul e neatomic: două predări înregistrate simultan primesc același număr, iar o alocare ștearsă logic decalează retroactiv toate numerele ulterioare. Un act de predare-primire cu răspundere materială nu poate avea număr dependent de cine îl privește.
  CORECTIE: Adaugă în migrare `document_type = 'proces_verbal_inventar'` și un RPC de rezervare (sau extinde `app.aloca_numar_inventar` cu parametru de tip), stochează numărul rezervat pe rândul de alocare într-o coloană dedicată și tipărește-l de acolo — niciodată recalculat la afișare.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: `descarcaRaportRespinse` construiește CSV-ul cu `"${r.erori.join(' | ').replace(/"/g, "''")}"`. Înlocuirea ghilimelei cu două apostrofuri NU este escapare CSV (regula este dublarea ghilimelei: `""`), iar `r.numarRand` nu e încadrat deloc. Mesajele de eroare încorporează conținut venit din fișierul importat, deci ostil: o celulă care conține un caracter de linie nouă rupe rândul raportului și permite injectarea de coloane/rânduri arbitrare, iar o celulă care începe cu `=`, `+`, `-` sau `@` devine formulă executabilă când operatorul deschide raportul în Excel (formula injection, cu acces la DDE/legături externe).
  CORECTIE: Escapează corect (`'"' + v.replace(/"/g,'""') + '"'` pentru fiecare câmp, inclusiv `numarRand`), elimină CR/LF din text și prefixează cu apostrof orice valoare care începe cu `=`, `+`, `-`, `@`, TAB sau CR.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: `inventory_allocations_select` cere `deleted_at is null`, dar `inventory_allocations_update` nu. Rândurile șterse logic rămân deci scriibile de cei care nu le mai pot citi: un angajat poate seta `confirmat_de_angajat_la` pe o predare anulată (rândul reintră în audit ca activitate reală), iar administratorul poate modifica alocări anulate fără ca ele să apară în vreo listă. Este forma clasică de divergență între ce se vede și ce se poate scrie.
  CORECTIE: Adaugă `and deleted_at is null` în clauza USING a politicii `inventory_allocations_update` (și în WITH CHECK, dacă ștergerea logică trebuie să rămână un act separat, rezervat administratorului).
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Ramura de angajat din `inventory_allocations_update` autorizează o SCRIERE printr-o permisiune de CITIRE: `employee_id = app.current_employee_id(organization_id) and app.can(organization_id, 'inventory', 'read', 'own')`. Portalul angajatului cere `inventory:read` la 'own' pentru toată lumea, deci în practică fiecare salariat capătă drept de UPDATE pe rânduri din `inventory_allocations`. Astăzi paguba e limitată de triggerul de coloane — dar triggerul e o listă neagră (vezi constatarea separată), iar dreptul rămâne acordat și dacă triggerul e vreodată modificat, dezactivat pentru o migrare, sau ocolit printr-o coloană nouă.
  CORECTIE: Introdu în vocabularul de permisiuni un drept propriu pentru confirmare (sau folosește `inventory:update` la scope 'own') și gardează ramura angajatului pe el, ca autorizarea de scriere să nu mai derive din dreptul de citire.

## produs
VERDICT: Baza de date este net mai matură decât produsul. Migrarea 0010 rezolvă corect problemele grele (dubla alocare prin constrângere de excludere, numerotare atomică, conectarea la motorul de expirări), dar interfața livrată nu duce niciuna dintre ele până la omul care lucrează cu ele.

Pentru magazioner, fluxul de bază nu se închide: predă obiectul și procesul-verbal nu apare nicăieri, refuzul la dubla alocare nu spune la cine e obiectul, returnarea unui laptop spart îl repune imediat pe lista celor disponibile, iar butonul care șterge fizic 300 de obiecte nu cere nicio confirmare. Importul cere un fișier fără să spună ce coloane trebuie să aibă, nu arată deloc ce urmează să intre în bază, și lasă în urmă loturi „în lucru” pe care nimeni nu le mai poate vedea sau anula.

Pentru angajat, modulul practic nu există. Portalul e anunțat în meniu și nu e livrat, iar confirmarea de primire — coloană, politică RLS, trigger de protecție pe coloană și index dedicat — nu are niciun buton. Toată acea infrastructură este cod mort până când apare un ecran.

Cel mai periculos defect tăcut: `angajati/[id]/bunuri/page.tsx` aruncă `error` de la toate interogările și afișează eșecul ca „Niciun obiect alocat”. Este exact ecranul pe care se face descărcarea de gestiune la plecarea unui angajat.

Ca să pot spune onest că Faza 5 e gata, mai trebuie, în ordinea importanței: (1) portalul angajatului plus acțiunea de confirmare a primirii, cu notificare la predare; (2) numărul de PV stocat pe rând, nu recalculat la tipărire; (3) tratarea explicită a erorilor pe fiecare interogare și `error.tsx` pe fiecare rută; (4) traducerea lui 23P01 într-un mesaj care numește deținătorul; (5) previzualizare reală, șablon descărcabil și ecran de loturi pentru import; (6) confirmări pe cele două operațiuni distructive (returnare, revocare lot). Rămân în afara acestei livrări, dar trebuie asumate explicit ca fiind pentru o fază următoare: exportul listei de inventar (contabilul îl cere lunar, iar `inventory:export` nici măcar nu există în `PermissionKey`), înregistrarea bunurilor pierdute cu imputare, casarea cu dată și motiv, și inventarierea anuală — pentru un magazioner român, sarcina lui cea mai grea din an și, deocamdată, complet absentă din modul.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/config/navigation.ts
  PROBLEMA: Jumătatea de produs care privește angajatul nu există. `PORTAL_NAV_ITEMS` primește intrarea „Obiectele mele” → `/portal/inventar`, dar în niciuna dintre cele trei livrări nu apare un fișier sub `src/app/(portal)/` sau `src/app/(app)/portal/inventar/`. Lista completă de fișiere produse conține doar `inventar/*`, `inventar/alocari/*`, `inventar/import/*` și `angajati/[id]/bunuri/*` — toate ecrane de birou. Scenariu: angajatul deschide portalul pe telefon, vede în meniu „Obiectele mele”, apasă și primește 404. Singurul loc unde își poate vedea bunurile este `/angajati/[id]/bunuri`, o rută de HR unde trebuie să-și ghicească propriul `employee_id` din URL.
  CORECTIE: Livrați `src/app/(portal)/portal/inventar/page.tsx`: citește alocările deschise ale angajatului curent (RLS la scope 'own' filtrează singură, fără `id` în URL), afișare pe carduri, nu tabel, pentru ecran de telefon. Până atunci scoateți intrarea din `PORTAL_NAV_ITEMS` — un meniu care duce în 404 e mai rău decât o funcție lipsă.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/alocari/actions.ts
  PROBLEMA: Confirmarea primirii este implementată integral în baza de date și nicăieri în aplicație. Migrarea are coloana `confirmat_de_angajat_la`, o politică de UPDATE care lasă anume angajatul să atingă rândul, un trigger care îi restrânge dreptul la exact acea coloană și chiar un index dedicat (`inventory_alloc_neconfirmate_idx`). Dar nicio acțiune `confirmaPrimirea` nu apare în lista de fișiere livrate, iar singurul ecran care atinge subiectul, `angajati/[id]/bunuri/page.tsx`, doar tipărește „Da”/„Nu” într-o celulă. Scenariu: magazionerul predă 40 de laptopuri, toate rămân „neconfirmate” la nesfârșit, fiindcă niciun angajat nu are pe unde să apese; la un litigiu, procesul-verbal nu are nici semnătură electronică, nici confirmare în sistem. Indexul `inventory_alloc_neconfirmate_idx` deservește un ecran care nu există.
  CORECTIE: Adăugați acțiunea `confirmaPrimireaObiectului({ allocationId })` (permission `inventory:read`, minScope 'own') și un buton „Confirm că am primit obiectul” în portalul angajatului, plus ecranul complementar pentru magazioner „Predări neconfirmate” care folosește indexul deja creat. Trimiteți și o notificare la predare (tabelele `notifications` / `notification_preferences` și `sendEmail` există deja) — altfel angajatul nu află niciodată că are ceva de confirmat.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/angajati/[id]/bunuri/page.tsx
  PROBLEMA: Erorile de interogare sunt înghițite și redate ca stare „gol”. Toate cele trei apeluri destructurează doar `data` — `const { data: alocari } = await supabase...`, la fel pentru `angajat` și `itemi` — și aruncă `error`. Scenariu: o eroare de rețea, un timeout sau o politică RLS care refuză rândul face `alocari` să fie `null`; pagina afișează liniștit „Niciun obiect alocat — Angajatul nu are în prezent niciun obiect de inventar în primire.” Magazionerul semnează lichidarea unui angajat care de fapt are în primire un laptop de 4.500 lei. Același tipar transformă o eroare pe `employees` în mesajul fals „Angajatul nu a fost găsit”. Semnalarea 6 din livrarea de alocări confirmă că paginile de listă folosesc „doar EmptyState pentru eroare”.
  CORECTIE: Destructurați `{ data, error }` la fiecare interogare, aruncați la `error` (ca să prindă `error.tsx`) sau randați un `ErrorState` distinct. Empty și error nu au voie să arate la fel niciodată — mai ales pe un ecran de descărcare de gestiune.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/lib/documents/proces-verbal.ts
  PROBLEMA: Numărul procesului-verbal se recalculează la fiecare tipărire în loc să fie stocat. Semnalarea 4 din livrarea PV descrie soluția aleasă: „un număr calculat (poziția alocării în anul predării, per organizație)”. `inventory_allocations` nu are nicio coloană `pv_numar`. Scenariu real: pe 10 martie se predă un laptop, se tipărește „PV nr. 12”, ambele părți semnează. Pe 15 martie se înregistrează retroactiv o predare din 3 martie (caz pe care migrarea îl tratează explicit ca legitim, prin constrângerea de excludere) sau se anulează logic o predare anterioară. Documentul semnat cu nr. 12 se retipărește ca nr. 13. La control de gestiune există pe hârtie două documente cu același număr și unul al cărui număr nu mai poate fi regăsit în sistem.
  CORECTIE: Adăugați `pv_numar text` și `pv_emis_la timestamptz` pe `inventory_allocations`, alocate O SINGURĂ DATĂ la prima generare, dintr-un `document_type = 'pv_inventar'` în `document_sequences` (extindeți `app.aloca_numar_inventar` cu un parametru de tip sau adăugați un RPC generic). Un număr de document nu se calculează niciodată la citire.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/alocari/formulare.tsx
  PROBLEMA: Procesul-verbal nu se oferă în momentul în care e nevoie de el. După o predare reușită, formularul face doar `router.refresh()` — nu redirecționează spre `/inventar/alocari/[id]/proces-verbal` și nu afișează niciun link „Tipărește procesul-verbal”. În `angajati/[id]/bunuri/page.tsx`, tabelul „În prezent la angajat” are coloanele Obiect / Predat la / Stare la predare / Confirmat și un singur link, spre fișa obiectului; PV-ul nu apare nicăieri. `RandAlocareDeschisa` are doar un select de stare și butonul „Returnează”. Scenariu: magazionerul predă laptopul cu angajatul în fața lui, ca să semneze pe loc; ecranul se reîmprospătează și atât — trebuie să caute singur ruta PV-ului, pe care nimic din interfață nu o anunță. Documentul se semnează a doua zi sau deloc.
  CORECTIE: După predare, redirecționați direct la PV (sau deschideți-l într-un tab nou) și adăugați o coloană cu link „PV” pe fiecare rând de alocare, în ambele tabele. Predarea și documentul ei sunt un singur act; nu se despart în două căutări.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Când obiectul e deja predat, sistemul refuză fără să spună la cine. Constrângerea `inventory_alloc_fara_suprapunere` întoarce SQLSTATE 23P01, iar comentariul din migrare recunoaște singur că traducerea rămâne de făcut: „în UI trebuie tradus SQLSTATE 23P01 într-un mesaj omenesc”. Nicio livrare nu conține acea traducere. În plus, nici mesajul propus acolo („Obiectul este deja predat altui angajat în perioada indicată”), nici mesajele din triggere („Obiectul este predat unui angajat. Înregistrați mai întâi returnarea…”) nu numesc persoana. Scenariu: magazionerul selectează monitorul DEMO-INV-00003, apasă „Predă” și primește fie un text Postgres brut cu numele constrângerii, fie un refuz care nu-l ajută — trebuie să deschidă altă pagină, să caute obiectul și să vadă cine îl are. Datele există: `gasesteDetinatorCurent` e deja scrisă în aceeași `actions.ts`.
  CORECTIE: Prindeți 23P01 în acțiunea de predare, chemați `gasesteDetinatorCurent(itemId)` și întoarceți: „Obiectul INV-2026-00042 este în primire la Ionescu Maria din 12.03.2026. Înregistrați mai întâi returnarea.” În plus, filtrați lista de obiecte din formular la `status = 'in_stoc'`, ca refuzul să fie excepția, nu fluxul normal.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Un obiect returnat defect redevine imediat disponibil pentru predare. În `internal.inventory_alloc_propaga`, la returnare nu mai există alocare deschisă, statusul anterior era 'alocat' (deci nu intră în ramura care păstrează 'casat'/'in_reparatie') și se ajunge la `else 'in_stoc'`, în timp ce `stare` primește 'defect' din `stare_la_returnare`. Scenariu: angajatul aduce laptopul cu ecranul spart, magazionerul consemnează corect starea „defect”, iar obiectul apare în aceeași secundă ca `in_stoc`. A doua zi, alt magazioner îl vede pe lista de disponibile și îl predă unui coleg nou-angajat. Nimic — nici constrângere, nici avertisment în interfață — nu împiedică asta.
  CORECTIE: În `inventory_alloc_propaga`, când `new.stare_la_returnare = 'defect'` treceți statusul pe 'in_reparatie', nu pe 'in_stoc'. Independent de asta, formularul de predare trebuie să excludă sau cel puțin să marcheze vizibil obiectele cu `stare in ('defect','uzat')`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/alocari/formulare.tsx
  PROBLEMA: Returnarea — act cu efect juridic — se declanșează dintr-un singur clic, fără confirmare și fără date. `RandAlocareDeschisa` are un `<select>` presetat pe 'bun' și un buton `px-2 py-0.5 text-xs` (≈20px înălțime) care apelează direct `returneazaObiectulDeInventar`. Nu există dialog de confirmare, nu există câmp de observații (coloana `observatii` există în tabelă), nu se generează PV de restituire și nu există nicio cale de anulare în interfață. Scenariu: magazionerul, pe tabletă în magazie, atinge din greșeală butonul de pe rândul vecin; laptopul lui Popescu apare returnat „în stare bună”, angajatul e descărcat de răspundere, obiectul intră în stoc, iar triggerul de audit consemnează operațiunea ca legitimă. Nu există buton de revenire.
  CORECTIE: Dialog de confirmare cu numele obiectului și al angajatului, câmp obligatoriu de observații când starea la returnare nu e identică cu cea de la predare, ținte de atingere de minimum 44px, și un PV de restituire generat la fel ca cel de predare.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: „Revocă lotul” șterge fizic sute de obiecte la un clic, fără confirmare și fără motiv. `revoca()` apelează direct `revocaImportInventar({ batchId })`; nu există dialog, nu se cere `motiv`, deși `app.revoca_import_inventar(p_batch_id, p_motiv)` are parametrul și îl salvează în `motiv_revocare`. Butonul stă lipit lângă „Aplică import”, la aceeași dimensiune. Scenariu: magazionerul tocmai a aplicat 300 de obiecte, vrea să închidă ecranul, apasă butonul roșu crezând că închide previzualizarea — `DELETE FROM inventory_items WHERE import_batch_id = ...` șterge fizic tot lotul (singura ștergere fizică din modul, cum spune migrarea) și interfața se resetează la pasul de încărcare, fără niciun mesaj despre ce s-a întâmplat.
  CORECTIE: Dialog de confirmare care afișează câte obiecte se șterg și cere motivul (text obligatoriu, transmis ca `p_motiv`), buton secundar vizual distinct, plus un mesaj de rezultat care spune câte obiecte au fost șterse. Ascundeți butonul complet după etapa 'finalizat', unde revocarea devine o operațiune rară, de administrare.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: Previzualizarea nu previzualizează nimic din ce va intra în baza de date. Ecranul afișează un rezumat numeric și un tabel exclusiv cu rândurile RESPINSE; `valide` este ținut în state doar ca să fie trimis înapoi la server. Butonul spune „Aplică import (300 obiecte)” fără ca magazionerul să fi văzut vreunul dintre ele. Al doilea defect: tabelul de respinse are doar coloanele „Rând” și „Erori”, deci pentru „rândul 47: dată achiziție invalidă” omul trebuie să redeschidă fișierul Excel și să numere rândurile ca să afle despre ce obiect fizic e vorba. Scenariu: o coloană mapată greșit face ca toate cele 300 de denumiri să intre în „model” și numerele de inventar să fie goale — nimic din ecran nu arată asta înainte de aplicare.
  CORECTIE: Tabel cu rândurile valide (denumire, număr de inventar, categorie, valoare), paginat sau virtualizat, ca previzualizare reală; în tabelul de respinse adăugați denumirea și numărul de inventar brut din fișier, lângă numărul rândului.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/actions.ts
  PROBLEMA: Loturile de import rămân orfane și invizibile. `pregatesteImportInventar` creează rândul din `inventory_import_batches` la încărcarea fișierului, cu `status = 'in_lucru'`, dar `batchId` trăiește exclusiv în state-ul React al componentei, iar semnalarea 9 din livrarea de alocări confirmă că nu există nicio pagină pentru loturi. Scenariu: magazionerul încarcă fișierul, vede că 120 din 300 de rânduri sunt respinse, închide tabul ca să corecteze Excelul. Lotul rămâne pentru totdeauna `in_lucru`, nu apare nicăieri în interfață, nu poate fi revocat (revocarea cere `batchId`, pe care nimeni nu-l mai poate obține) și nu se poate reveni la el. La a treia încercare există trei loturi fantomă, iar `randuri_total` din rapoarte devine neinterpretabil.
  CORECTIE: Livrați `/inventar/import/loturi` (listă cu status, fișier, data, rânduri, buton de revocare) și reluați acolo un lot rămas `in_lucru`. Alternativ, creați rândul de lot abia la aplicare și țineți previzualizarea complet în memorie.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: Ecranul de import nu spune ce trebuie încărcat. Etapa 'incarcare' e un `<div>` punctat care conține doar `<input type="file" accept=".xlsx,.xls">` — niciun titlu, nicio instrucțiune, nicio listă de coloane obligatorii, niciun șablon descărcabil și niciun `<label>` legat de input (deci și fără nume accesibil: un cititor de ecran anunță „buton încărcare fișier”, atât). Scenariu: magazionerul are un export din programul vechi cu antetele „Cod”, „Denumire mijloc”, „Valoare inv.”; le încarcă, primește 300 de rânduri respinse cu erori de mapare și nu are de unde să afle cum trebuie numite coloanele. Suplimentar, `accept` refuză CSV-ul, formatul în care exportă cele mai multe programe vechi de gestiune (iar raportul de erori generat de aplicație e tot CSV).
  CORECTIE: Titlu, paragraf cu coloanele obligatorii și opționale, buton „Descarcă șablonul Excel”, `<label htmlFor>` pe input și acceptarea `.csv`. Fără șablon, un import de 300 de rânduri este garantat o sesiune de încercări repetate.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/import/import-client.tsx
  PROBLEMA: Raportul de erori descărcabil se deschide stricat în Excel-ul unui utilizator român. `descarcaRaportRespinse` scrie separator virgulă și `Blob` fără BOM UTF-8. Scenariu: magazionerul dublu-clic pe `raport-import-inventar-respinse.csv`; Excel cu setări româno-locale folosește punct și virgulă ca separator de listă, deci tot conținutul intră într-o singură coloană, iar fără BOM diacriticele din mesajele de eroare („Numărul de inventar există deja”, „Data achiziției…”) devin caractere ilizibile. În plus, escaparea e greșită: `.replace(/"/g, "''")` înlocuiește ghilimeaua cu două apostrofuri în loc să o dubleze, alterând textul erorii.
  CORECTIE: Prefixați conținutul cu `﻿`, folosiți `;` ca separator (sau exportați direct .xlsx, coerent cu formatul cerut la intrare) și dublați ghilimelele: `.replace(/"/g, '""')`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Ghilimele amestecate în mesaje care ajung pe ecranul magazionerului. În `internal.inventory_items_valideaza`, mesajul este „Obiectul nu poate fi marcat „alocat" fără o predare-primire înregistrată.” — deschidere U+201E corectă, închidere ghilimea dreaptă ASCII. Același tipar apare în comentariul funcției `app.aloca_numar_inventar` („INV-2026-00042") și în comentariile de index. Scenariu: magazionerul încearcă să corecteze manual statusul unui obiect, primește mesajul P0001 afișat literal în interfață și vede un text pe care orice om obișnuit cu documente românești îl citește ca neterminat. Regula proiectului cere pereche „…” (U+201E / U+201D).
  CORECTIE: Închideți toate perechile cu U+201D în mesajele de excepție și în comentariile de schemă; verificați cu o căutare pe U+201E neurmat de U+201D în același șir.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/config/navigation.ts
  PROBLEMA: Trei vocabulare diferite pentru același lucru. Meniul spune „Predare-primire”, ruta și entitatea se numesc „alocări”, statusul obiectului este „alocat”, iar tabelele de pe ecranul angajatului au antetul „Istoric predări-primiri”. „Alocare” este jargon de IT: un magazioner român spune „predare-primire”, „dat în folosință”, „obiect în primire la…”. Tot așa, „Import loturi” și „Revocă lotul” sunt formulări de programator — omul înțelege „Import din Excel” și „Anulează importul”. Scenariu: magazionerul caută în meniu unde vede cine ce are; găsește „Predare-primire”, dar coloana din tabel și mesajele de eroare vorbesc despre „alocări”, iar la telefon, când sună contabila și întreabă „obiectul e dat în folosință?”, el caută un cuvânt care nu apare pe ecran.
  CORECTIE: Un singur termen în tot produsul: „predare-primire” pentru act, „în primire la X” pentru stare, „în magazie” în loc de „în stoc”. Redenumiți etichetele din `INVENTORY_STATUS_LABEL` și titlurile de meniu; ruta poate rămâne `/inventar/alocari`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0010_inventory.sql
  PROBLEMA: Ciclul real de viață al obiectului nu poate fi consemnat. `inventory_item_stare` are doar nou/bun/uzat/defect — nu există „pierdut” sau „lipsă”, iar `inventory_item_status` nu are „lipsă la inventar”. Scenariu 1: la lichidare, angajatul nu mai găsește telefonul de serviciu; magazionerul nu poate închide alocarea decât mințind (returnat „defect”) sau lăsând-o deschisă la un om care nu mai lucrează în firmă — și blocând obiectul prin constrângerea de excludere. Nu se poate porni nicio imputare. Scenariu 2: casarea. Antetul migrării invocă „raportul de casare” ca justificare a deciziei de a nu avea `deleted_at`, dar `inventory_items` nu are `data_casare`, `motiv_casare` sau numărul PV-ului de casare, și nu există ecran de casare — „casat” este un status fără dată și fără motiv, recuperabil doar scotocind în `audit_logs`.
  CORECTIE: Adăugați valorile de stare/status pentru bun nerecuperat, plus `data_casare date`, `motiv_casare text` și `pv_casare_numar text`, cu un ecran de casare care le cere. Raportul de casare pe care se sprijină decizia de schemă nu se poate produce fără ele.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/inventar/alocari/formulare.tsx
  PROBLEMA: Accesibilitate și stări de ecran incomplete. `RandAlocareDeschisa` are un `<select>` fără etichetă și fără `aria-label` (un cititor de ecran anunță doar „listă” pe fiecare din cele N rânduri deschise), iar mesajul de eroare este un `<span>` fără `role="alert"`, deci eșecul unei returnări nu e anunțat deloc; ținta de atingere a butonului este sub 24px. Separat, `loading.tsx` există doar pentru `inventar/`, `inventar/alocari/` și `angajati/[id]/bunuri/`, lipsind pe `inventar/import/`, `inventar/[id]/`, `inventar/nou/` și `inventar/alocari/[id]/proces-verbal/`; niciun `error.tsx` nu există nicăieri — semnalarea 6 din livrarea de alocări o recunoaște explicit. Scenariu: interogarea de pe fișa obiectului eșuează, utilizatorul primește ecranul generic de eroare al aplicației, fără context și fără buton de reîncercare, așa cum cere specificația.
  CORECTIE: `aria-label` pe select, `role="alert"` pe mesajele de eroare, ținte de minimum 44px pentru uz în magazie, `loading.tsx` pe rutele lipsă și `error.tsx` cu buton de reîncercare pe fiecare segment de rută al modulului (confirmați întâi semnătura reală a `ErrorState`).