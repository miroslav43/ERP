## corectitudine
VERDICT: Arhitectura de fond este corectă şi răspunde bine la întrebările-cheie ale lentilei: reînnoirea unui document ÎNAINTE de expirare chiar funcţionează (nu există EXCLUDE pe suprapunere, indexul unic e parţial pe `este_curent`, iar cele două UPDATE-uri separate evită coliziunea tranzitorie); documentul curent se alege corect după `expira_la` MAXIM, nu după ultimul inserat, deci o poliţă introdusă retroactiv nu dă afară poliţa validă; sincronizarea foloseşte `internal.sync_expirable()` cu `kind` = codul tipului şi `entity_id` = vehiculul, fără logică de alerte duplicată; popularea iniţială există şi e reutilizabilă; `km_parcursi` este GENERATED şi rămâne NULL la cursele în desfăşurare, iar consumul este păzit de `if (totalKm > 0 && totalLitri > 0)`, deci nu există împărţire la zero. Ce strică livrarea sunt trei lucruri de integrare: (1) migrarea nu se aplică — `app.can(...)` cu 4 argumente nu există în inventarul de helperi SQL şi apare în ~12 politici, iar semnătura lui `internal.sync_expirable()` a fost ghicită, nu citită; (2) `entity_type = 'vehicle_document'` NU este înregistrat în `app.poate_vedea_expirabil()`, contrar cerinţei explicite — se afirmă doar că 0008 l-ar avea deja, cu un simplu `raise warning` drept plasă, ceea ce face ca eşecul (scadenţe invizibile în dashboard) să fie complet tăcut; (3) verificarea de regres kilometric rulează la fiecare UPDATE şi blochează definitiv foile aprobate în altă ordine decât cea cronologică — nu se mai pot aproba, respinge sau şterge. La acestea se adaugă un strat de UI care nu compilează (`@/schemas/fleet` şi componentele de formular nu au fost livrate) şi rute de meniu care duc în pagina de eroare, nu în 404.
- [CRITIC] supabase/migrations/0012_fleet.sql
  PROBLEMA: `app.can(uuid, text, text, text)` NU există în inventarul de helperi SQL (acolo apar doar `app.has_permission(uuid, text, text)`, `app.is_member`, `app.feature_on`, `app.current_employee_id`, `app.is_manager_of`, `app.is_service_context`). Funcţia e folosită în ~12 locuri: `vdt_select`, `foi_insert`, `foi_update`, `alimentari_insert/update`, `anomalii_update` şi în `internal.foi_parcurs_inainte` (verificarea dreptului de aprobare). `app.can` pare împrumutat din helperul TypeScript `can` din @/lib/auth/permissions, care nu are un geamăn SQL. Suplimentar, `app.has_permission(...)` este folosită ca şi cum ar returna un scope text ('all'/'team'/'own'), ipoteză neverificată — numele sugerează boolean.
  CORECTIE: Migrarea eşuează la aplicare cu „function app.can(uuid, text, text, text) does not exist” la prima politică care o foloseşte; întreaga Faza 8 nu se instalează. Înlocuiţi fiecare apel cu `app.has_permission(org, 'resursa', 'actiune')` comparat explicit cu scope-ul dorit (după confirmarea tipului de retur din 0001/0002), sau definiţi `app.can(uuid, text, text, text)` chiar în 0012, cu SECURITY DEFINER şi `set search_path = ''`.
- [CRITIC] supabase/migrations/0012_fleet.sql
  PROBLEMA: În `internal.foi_parcurs_inainte()` verificarea de regres (`new.km_plecare < v_ultim`) rulează la FIECARE UPDATE, fără nicio exceptare pentru schimbarea de stare, respingere sau ştergere logică (`new.deleted_at is not null` nu este exclus). `v_ultim` include `max(km_sosire)` peste toate foile aprobate ale vehiculului plus `vehicles.km_curent`, deci creşte în timp independent de foaia editată.
  CORECTIE: Scenariu: foaia A (100→200 km) şi foaia B (200→300 km) sunt ambele „trimis”. Aprobatorul o aprobă întâi pe B → `km_curent` devine 300. Din acel moment ORICE UPDATE pe A ridică P0001 „Kilometrajul de plecare (100 km) este mai mic decât ultimul kilometraj cunoscut (300 km)”: nu se mai poate aproba, nu se mai poate respinge şi nu se mai poate şterge logic. Foaia rămâne blocată definitiv în „trimis”, iar singura ieşire e falsificarea km_plecare. La fel se blochează orice ciornă mai veche pe un vehicul de pool după ce foaia altui şofer a fost aprobată. Verificarea de regres trebuie făcută doar la INSERT şi la tranziţia draft→trimis, raportat la kilometrajul de la momentul întocmirii (sau ignorată când `new.deleted_at is not null` ori când doar `status`/`motiv_respingere` se schimbă).
- [CRITIC] supabase/migrations/0012_fleet.sql
  PROBLEMA: Sarcina cerea explicit „Alege un entity_type nou şi ÎNREGISTREAZĂ-L în app.poate_vedea_expirabil() — implicitul este restrictiv”. Migrarea nu înregistrează nimic: comentariul din antet afirmă că `entity_type = 'vehicle_document'` ar fi „DEJA înregistrat în 0008, ramura when 'vehicle_document'” — o afirmaţie despre cod existent care nu poate fi susţinută (0008 este migrarea motorului generic de expirări, scrisă înainte de Faza 8). Singura plasă de siguranţă este un `raise warning` la finalul fişierului, care nu opreşte nimic şi nu ajunge la nimeni.
  CORECTIE: Dacă ramura nu există în 0008, toate scadenţele de flotă sunt sincronizate corect în `expirables` dar INVIZIBILE în dashboardul de conformitate (implicit restrictiv) — eşec complet tăcut al integrării, exact ce avertiza briefingul. În plus, `0012b_fleet_expirables.sql` transformă aceeaşi condiţie în `raise exception`, deci a doua migrare ar aborta imediat după ce prima a trecut cu un simplu warning — incoerenţă. Adăugaţi în 0012 înregistrarea propriu-zisă (CREATE OR REPLACE peste `app.poate_vedea_expirabil()` cu ramura `when 'vehicle_document' then app.poate_vedea_vehicul(...)`, păstrând ramurile existente) şi transformaţi warning-ul în `raise exception`.
- [MARE] supabase/migrations/0012_fleet.sql
  PROBLEMA: `internal.sync_expirable()` este apelată cu 9 argumente poziţionale ghicite (org, entity_type, entity_id, kind, etichetă, expires_at, tabelă-sursă 'vehicle_documents', employee_id, is_active), fără ca semnătura reală din 0008 să fi fost citită. Se presupune în plus că `p_expires_at` acceptă NULL cu semantica „scadenţa dispare logic şi alertele deschise se închid”.
  CORECTIE: O singură nepotrivire de aritate, ordine sau tip face să eşueze `create function internal.flota_sincronizeaza_grup` (la prima execuţie) şi apelul `select internal.flota_resincronizeaza_expirari()` de la finalul migrării, deci întreaga instalare. Dacă `expirables.expires_at` este NOT NULL, ştergerea logică a ultimului document al unui grup ridică o eroare în plin UPDATE de utilizator. Citiţi semnătura din supabase/migrations/0008_expirables.sql şi treceţi la apel cu parametri numiţi (`p_organization_id => ...`), ca o schimbare de ordine să nu treacă tăcut.
- [MARE] supabase/migrations/0012_fleet.sql
  PROBLEMA: Politica `anomalii_insert` cere simultan `app.is_service_context()` ŞI `organization_id = any((select app.current_org_ids())::uuid[])`. Într-un context de serviciu nu există `auth.uid()`, deci `app.current_org_ids()` întoarce un tablou gol — cele două condiţii se exclud reciproc. Tabela are FORCE ROW LEVEL SECURITY, iar singurul scriitor real este triggerul SECURITY DEFINER `internal.foi_parcurs_dupa()`.
  CORECTIE: Nimeni nu poate satisface WITH CHECK-ul: inserarea anomaliilor funcţionează doar dacă rolul proprietar al funcţiei are BYPASSRLS — o presupunere nescrisă. Dacă proprietarul se schimbă (sau FORCE RLS e evaluat), la prima foaie cu salt de kilometraj UPDATE-ul/INSERT-ul şoferului eşuează cu „new row violates row-level security policy for table odometer_anomalies”, adică o avertizare de kilometraj devine un refuz de salvare — exact comportamentul pe care decizia 3 din antet spunea că îl evită. Scoateţi condiţia de organizaţie din ramura de serviciu (`app.is_service_context() and deleted_at is null and confirmat_de is null`) sau inseraţi anomalia dintr-o funcţie care nu depinde de RLS, marcată explicit.
- [MARE] supabase/migrations/0012_fleet.sql
  PROBLEMA: `internal.vehicles_normalizeaza()` rulează BEFORE INSERT OR UPDATE şi revalidează la fiecare UPDATE că `employee_id` este un angajat activ (`deleted_at is null`) al organizaţiei. Dar UPDATE-ul de kilometraj din `internal.foi_parcurs_dupa()` (`update public.vehicles set km_curent = new.km_sosire ...`) trece prin acelaşi trigger.
  CORECTIE: Scenariu: maşina B100XYZ e alocată şoferului Ionescu; Ionescu pleacă din firmă şi este şters logic din `employees`. Următoarea foaie de parcurs aprobată pentru acel vehicul (întocmită de alt şofer) declanşează bump-ul de km_curent → triggerul ridică P0001 „Angajatul ales pentru vehicul nu aparţine organizaţiei dumneavoastră.” → APROBAREA FOII EŞUEAZĂ, cu un mesaj care nu are nicio legătură cu ce a făcut aprobatorul. Validaţi apartenenţa angajatului doar când `employee_id` chiar se schimbă: `if tg_op = 'INSERT' or new.employee_id is distinct from old.employee_id then ... end if`.
- [MARE] supabase/migrations/0012_fleet.sql
  PROBLEMA: `internal.vdoc_inainte()` cere ca tipul de document să fie `activ` şi nemarcat ca şters şi la UPDATE, nu doar la INSERT. Acelaşi trigger se aplică şi UPDATE-urilor interne de `este_curent` emise de `internal.flota_sincronizeaza_grup()`.
  CORECTIE: Scenariu: firma dezactivează tipul „casco” (activ = false) pentru că nu mai asigură CASCO. De atunci: (a) documentele CASCO existente nu mai pot fi şterse logic şi nu li se mai poate corecta nimic — orice UPDATE ridică „Tipul de document ales nu este disponibil sau a fost dezactivat”; (b) mai grav, dacă recalcularea are de întors un steag `este_curent` pe un document de tip dezactivat, UPDATE-ul intern eşuează şi aruncă eroarea în sus, blocând operaţia care a declanşat-o (ştergerea logică a unui document, marcarea vehiculului ca vândut prin `vehicles_dupa`). Aplicaţi verificarea de tip activ doar la `tg_op = 'INSERT'` sau când `document_type_id` se schimbă.
- [MARE] src/app/(app)/flota/page.tsx
  PROBLEMA: Rutele livrate sunt `/flota` şi `/flota/[id]/documente`. Butonul „+ Adaugă vehicul” trimite la `/flota/nou`, iar meniul propus în src/config/navigation.ts trimite la `/flota/vehicule`, `/flota/foi-parcurs`, `/flota/documente`, `/flota/alimentari`, `/portal/flota` — niciuna nu există. Toate cad pe segmentul dinamic `/flota/[id]`.
  CORECTIE: `src/app/(app)/flota/[id]/page.tsx` execută `.eq('id', 'nou')` pe o coloană uuid → PostgREST întoarce 22P02 (invalid input syntax for type uuid) → `if (error) throw new Error('Nu am putut încărca vehiculul.')` → ecran de eroare 500, nu 404. Deci butonul principal al modulului şi patru intrări de meniu duc într-o pagină de eroare. Livraţi `/flota/nou` şi aliniaţi rutele din navigaţie la arborele real; în plus, validaţi `id` ca uuid în `[id]/page.tsx` şi apelaţi `notFound()` în loc de `throw` pentru identificatori nevalizi.
- [MARE] src/app/(app)/flota/[id]/page.tsx
  PROBLEMA: Livrarea referă module care nu au fost livrate: `@/schemas/fleet` (VEHICLE_CATEGORII, VEHICLE_STATUSURI, ETICHETE_CATEGORIE, ETICHETE_STATUS, ETICHETE_COMBUSTIBIL, vehicleCreateSchema, vehicleUpdateSchema, vehicleDocumentCreateSchema), `../_components/vehicle-form`, `../_components/status-form`, acţiunile pe care le apelează acestea, `src/lib/queries/fleet.ts` (anunţat „în răspunsul următor”) şi `src/domain/fleet/consum.ts` — fişierul de test `consum.test.ts` importă `calculeazaConsum` şi `PRAG_ABATERE_CONSUM_IMPLICIT` dintr-un modul care lipseşte cu totul.
  CORECTIE: `next build` şi `tsc --noEmit` eşuează pe „Cannot find module '@/schemas/fleet'” şi pe cele două componente; suita de teste eşuează la import. Nimic din Faza 8 nu compilează în forma livrată. Fie livraţi fişierele lipsă, fie tăiaţi importurile şi secţiunile care le folosesc din paginile predate.
- [MARE] tests/rls/flota.test.ts
  PROBLEMA: `const roluriDupaRang = [...APP_ROLES].sort((a, b) => RANK[a] - RANK[b]);` foloseşte `RANK` din @/config/permissions ca şi cum ar fi un clasament de roluri. În inventar `RANK` apare între `PermissionScope, MinScope, RANK, meetsScope` — este clasamentul de SCOPE-uri (own/team/all), nu de AppRole.
  CORECTIE: Indexarea unui `Record<PermissionScope, number>` cu un `AppRole` este eroare de tipuri; chiar dacă tipul ar fi permisiv, `noUncheckedIndexedAccess` face `RANK[a]` să fie `number | undefined`, iar scăderea nu compilează. La runtime comparatorul ar întoarce NaN şi `ROL_SOFER` ar fi un rol arbitrar, deci fixture-ul ar acorda permisiunile altui rol decât cel testat şi testele de RLS ar trece/pica din motive false. Alegeţi rolul explicit (o constantă din APP_ROLES verificată cu `isAppRole`), nu prin sortare.
- [MEDIU] src/app/(app)/flota/page.tsx
  PROBLEMA: Funcţia `culoareScadenta` cu pragurile 14 şi 30 de zile este copiată identic în trei fişiere (`flota/page.tsx`, `flota/[id]/page.tsx`, `flota/[id]/documente/page.tsx`) şi codifică praguri de alertă în interfaţă, deşi briefingul spune explicit „NU duplica logica de praguri — există deja” (`alert_rules` din 0008), iar S13 interzice valorile hardcodate.
  CORECTIE: Un client care îşi configurează în `alert_rules` praguri de 45/20/5 zile pentru RCA va vedea în modulul de flotă un semafor verde pentru un document pe care motorul de conformitate îl semnalează deja roşu — două adevăruri diferite în aceeaşi aplicaţie. Extrageţi o singură funcţie într-un modul comun care citeşte pragurile din `alert_rules`/`expirables` (sau, minimal, dintr-o singură constantă partajată) şi importaţi-o în cele trei pagini.
- [MEDIU] supabase/migrations/0012_fleet.sql
  PROBLEMA: Nomenclatorul are două indexuri unice separate: `vdt_platforma_cod_uq` pe (cod) unde organization_id is null şi `vdt_org_cod_uq` pe (organization_id, cod). Nimic nu împiedică o firmă să-şi creeze un tip propriu cu un cod identic cu unul de platformă (de ex. 'rca'). `vdoc_curent_uq` este pe `document_type_id`, deci ambele documente pot fi `este_curent`, dar proiecţia în `expirables` foloseşte drept `kind` doar `cod`.
  CORECTIE: Scenariu: administratorul firmei nu vede în listă tipul de platformă şi îşi creează „RCA” propriu; vehiculul are acum documentul RCA-platformă (expiră 1 dec) şi RCA-firmă (expiră 1 mar). Ambele grupuri scriu în aceeaşi cheie `expirables(org, 'vehicle_document', vehicle_id, 'rca')` şi se suprascriu reciproc la fiecare salvare — scadenţa afişată în dashboard depinde de cine a fost salvat ultimul, iar alertele oscilează. Fie interziceţi în `internal.vdt_normalizeaza()` codurile care există deja ca rânduri de platformă, fie prefixaţi `kind` cu tipul (de ex. `cod || '_' || left(document_type_id::text, 8)` pentru rândurile proprii).
- [MEDIU] supabase/migrations/0012_fleet.sql
  PROBLEMA: `internal.foi_parcurs_dupa()` inserează anomalia de salt cu `on conflict (trip_sheet_id, tip) do nothing` şi nu o actualizează şi nu o închide niciodată dacă foaia se corectează. `internal.anomalii_protejeaza()` interzice explicit modificarea lui `km_declarat`/`km_asteptat`.
  CORECTIE: Scenariu: şoferul tastează 120000 în loc de 12000 la plecare, salvează ciorna → se creează o anomalie de salt. Îşi vede greşeala şi corectează imediat la 12000 → la UPDATE, condiţia de salt nu mai e îndeplinită, dar anomalia veche rămâne pe listă cu cifre care nu mai există nicăieri în sistem, iar responsabilul de flotă nu o poate corecta, doar confirma. Ştergeţi logic anomalia de tip 'salt' asociată foii când condiţia nu mai este îndeplinită, sau faceţi `on conflict ... do update set km_asteptat = excluded.km_asteptat, km_declarat = excluded.km_declarat` şi ştergeţi-o când diferenţa scade sub prag.
- [MEDIU] supabase/seed-flota.sql
  PROBLEMA: Blocul de gardă `do $$ ... where organization_id = :org_id ... $$;` foloseşte o variabilă psql în interiorul unui şir dollar-quoted. psql NU face interpolare de variabile în interiorul şirurilor dollar-quoted — textul `:org_id` ajunge ca atare la server.
  CORECTIE: PL/pgSQL ridică „syntax error at or near ":"”. Cum scriptul nu setează ON_ERROR_STOP, psql raportează eroarea şi CONTINUĂ: garda „organizaţia trebuie să aibă cel puţin 2 angajaţi” nu se execută niciodată, iar INSERT-urile care urmează cad pe un `employee_id` NULL luat din subinterogarea cu `offset 1` (coloană NOT NULL în trip_sheets) sau creează foi fără şofer. Mutaţi verificarea în SQL simplu (`select case when (select count(*) ...) < 2 then ... end`) sau construiţi blocul cu `\gset` + `format()`, şi adăugaţi `\set ON_ERROR_STOP on` la începutul fişierului.
- [MEDIU] supabase/migrations/0012b_fleet_expirables.sql
  PROBLEMA: Prima linie a fişierului este `// supabase/migrations/0012b_fleet_expirables.sql`. `//` nu este comentariu în PostgreSQL (comentariul de linie este `--`). Aceeaşi problemă la `supabase/seed-flota.sql`.
  CORECTIE: Ambele fişiere eşuează la prima linie cu „syntax error at or near "/"”, deci migrarea de verificare şi seed-ul demo nu rulează deloc, indiferent de conţinutul lor. Înlocuiţi `//` cu `--` pe prima linie a fiecărui fişier .sql (0012_fleet.sql este corect, începe cu `--`).
- [MEDIU] supabase/migrations/0012_fleet.sql
  PROBLEMA: În `internal.flota_sincronizeaza_grup()`, UPDATE-ul care stinge steagul `este_curent` are în WHERE `and d.deleted_at is null`, deci ocoleşte exact rândul tocmai şters logic. La ştergerea logică a documentului curent, acesta rămâne cu `este_curent = true` (triggerul BEFORE păstrează `old.este_curent` în afara recalculării).
  CORECTIE: Scenariu: RCA-ul curent a fost introdus greşit şi se şterge logic. În tabelă rămân două rânduri cu `este_curent = true` — cel şters şi noul curent. Indexul unic parţial nu semnalează (exclude rândurile şterse), interfaţa filtrează după `deleted_at is null`, dar orice raport, export sau interogare viitoare care se bazează pe „un singur document curent per vehicul+tip” numără dublu. Scoateţi `d.deleted_at is null` din UPDATE-ul de stingere sau adăugaţi explicit `set este_curent = false` pe rândul care tocmai a primit `deleted_at`.
- [MEDIU] supabase/migrations/0012_fleet.sql
  PROBLEMA: `internal.alimentari_inainte()` blochează modificarea alimentărilor pe o foaie aprobată doar când `new.deleted_at is null`. Ştergerea logică fiind tot un UPDATE cu `deleted_at` nenul, ea trece de gardă, iar politica `alimentari_update` cere doar `trip_sheets:update` la scope 'own' — adică chiar şoferul foii.
  CORECTIE: Scenariu: şoferul îşi trimite foaia, aceasta este aprobată cu 3 bonuri de combustibil, apoi el şterge logic două dintre ele. Foaia rămâne „aprobată” ca document justificativ, dar consumul calculat pe fişa vehiculului (litri / km × 100) scade retroactiv, fără urmă vizibilă în ecran. Contrazice direct regula proprie „o foaie aprobată este un document justificativ: cifrele ei nu se mai ating”. Blocaţi orice UPDATE (inclusiv ştergerea logică) pe alimentările unei foi aprobate, sau cereţi pentru ştergere permisiunea `trip_sheets:approve`.

## securitate
VERDICT: Modulul este bine gândit la nivel de tabele, triggere şi soft delete, dar are două găuri de autorizare care îl fac inacceptabil în forma actuală. (1) Migrarea foloseşte `app.can(uuid,text,text,text)` şi `app.azi_local()`, care nu figurează în inventarul de helperi SQL — dacă lipsesc, `CREATE POLICY` eşuează şi faza 8 nu se instalează deloc; asta trebuie verificat înainte de orice altceva. (2) Dreptul de scriere este derivat din scope-ul de CITIRE: `app.poate_vedea_foaie()`/`poate_vedea_vehicul()` comută pe permisiunea `read`, dar sunt singura restricţie de rând în politicile de INSERT/UPDATE, aşa că o combinaţie foarte banală (read:all + update:own) dă drept de modificare şi de ştergere logică asupra foilor şi alimentărilor întregii organizaţii. La răspunsul concret al lentilei: da, un şofer poate ajunge la foile altui şofer (prin punctul 2 şi prin lipsa verificării vehiculului la foi_insert); nu se poate auto-aproba la scope 'own'/'team', dar cine are approve:'all' îşi semnează propriile foi, iar approve:'team' aprobă toată firma fiindcă nimeni nu verifică `app.is_manager_of`. Portiţa `new.deleted_at is null` din cele două triggere anulează în practică imutabilitatea foilor aprobate şi a alimentărilor decontate. Castul `::uuid[]` şi parantezarea OR-urilor sunt corecte peste tot, funcţiile noi au `search_path = ''` şi REVOKE/GRANT, iar zerofierea câmpurilor calculate în BEFORE este coerentă cu WITH CHECK — dar `app.vehicul_sofer`/`app.foaie_sofer` sunt definer fără filtru de organizaţie, WITH CHECK-urile de UPDATE nu fixează `organization_id`, `created_by`/`created_at`/`aprobat_de`, iar pentru bonurile şi documentele din Storage nu există niciun contract de cale — calea vine de la client, deci o organizaţie poate referi obiectul alteia. Recomandare: nu se merge mai departe până nu sunt rezolvate cele două CRITIC şi cele şase MARE.
- [CRITIC] supabase/migrations/0012_fleet.sql (secţ. 9-10, toate politicile + internal.vehicles_normalizeaza)
  PROBLEMA: Migrarea se sprijină pe două funcţii SQL care NU apar în inventarul de helperi din schema `app`: `app.can(uuid, text, text, text)` (folosită în vdt_select, foi_insert, foi_update, alimentari_insert/update, anomalii_update şi în triggerul de aprobare) şi `app.azi_local()` (în internal.vehicles_normalizeaza). Inventarul listează doar app.is_platform_admin, current_org_ids, is_member, has_role, feature_on, has_permission(uuid,text,text), current_employee_id, is_manager_of, is_service_context, write_audit, attach_audit. Scenariu: funcţiile referite într-o politică se rezolvă la `CREATE POLICY`, deci dacă `app.can` lipseşte migrarea 0012 se opreşte cu „function app.can(uuid, text, text, text) does not exist" şi NIMIC din faza 8 nu se instalează; `app.azi_local()` ar crăpa mai târziu, la runtime, exact când cineva marchează un vehicul vândut sau casat.
  CORECTIE: Verificaţi în 0001-0008 dacă `app.can` şi `app.azi_local` există cu exact aceste semnături. Dacă nu: fie le definiţi explicit în 0012 (`app.can(p_org uuid, p_resource text, p_action text, p_min text) returns boolean`, SECURITY DEFINER, `set search_path = ''`, cu REVOKE de la public/anon şi GRANT la authenticated), fie rescrieţi toate apelurile pe `app.has_permission(...)` cu comparaţie de rang explicită, iar `app.azi_local()` pe `(now() at time zone 'Europe/Bucharest')::date`.
- [CRITIC] supabase/migrations/0012_fleet.sql (app.poate_vedea_foaie / app.poate_vedea_vehicul + politicile foi_insert, foi_update, alimentari_insert, alimentari_update)
  PROBLEMA: Dreptul de SCRIERE este derivat din scope-ul de CITIRE. `app.poate_vedea_foaie()` comută pe `app.has_permission(org, 'trip_sheets', 'read')`, dar este folosită ca singură restricţie de rând în politicile de INSERT şi UPDATE, alături de un `app.can(..., 'update', 'own')` care verifică doar EXISTENŢA dreptului, nu şi rândul. Scenariu real: un rol foarte obişnuit — dispecer/HR cu `trip_sheets:read = all` şi `trip_sheets:update = own` — trece prin USING pentru ORICE foaie din organizaţie (read all ⇒ poate_vedea_foaie = true) şi poate modifica km, traseu, motiv, poate trimite spre aprobare sau soft-şterge foile oricărui şofer. Acelaşi lucru pentru alimentări. În plus, `alimentari_update` are WITH CHECK FĂRĂ `poate_vedea_foaie`, deci o alimentare poate fi re-pointată pe trip_sheet_id-ul altui şofer (triggerul verifică doar organizaţia şi statusul foii), mutând consumul de la un şofer la altul.
  CORECTIE: Separaţi scope-ul de scriere de cel de citire: adăugaţi `app.poate_scrie_foaie(p_org uuid, p_sofer_id uuid)` care comută pe `app.has_permission(org,'trip_sheets','update')` (respectiv `'create'` pentru INSERT) şi folosiţi-o în foi_insert/foi_update/alimentari_*. Repetaţi tiparul pentru vehicule (`vehicles:update`, nu `vehicles:read`). Completaţi WITH CHECK-ul de la alimentari_update cu aceeaşi condiţie ca USING (S8).
- [MARE] supabase/migrations/0012_fleet.sql (foi_update + internal.foi_parcurs_inainte, ramura de aprobare)
  PROBLEMA: „Team" înseamnă practic „all" la aprobare. Nici politica (`app.can(organization_id,'trip_sheets','approve','team')`, fără nicio legătură cu şoferul), nici triggerul (acelaşi apel, urmat doar de verificarea auto-aprobării) nu verifică `app.is_manager_of(org, new.employee_id)`. Scenariu: şeful de coloană din depozitul A, cu approve scope 'team', aprobă foile şoferilor din depozitul B — şi, prin ramura de OR din USING, le poate şi modifica orice câmp înainte de aprobare. Separat: verificarea de auto-aprobare cade explicit când scope-ul e 'all' (`app.has_permission(...) <> 'all'`), deci cine are approve:all îşi semnează propriile foi de parcurs — un document justificativ fără segregarea sarcinilor.
  CORECTIE: În trigger, la aprobare: dacă scope-ul e 'team', cereţi în plus `app.is_manager_of(new.organization_id, new.employee_id)`; dacă e 'own', refuzaţi. În foi_update, restrângeţi ramura de aprobator cu `app.poate_vedea_foaie(organization_id, employee_id)`. Blocaţi auto-aprobarea indiferent de scope (excepţia rămâne doar `app.is_service_context()`).
- [MARE] supabase/migrations/0012_fleet.sql (internal.foi_parcurs_inainte şi internal.alimentari_inainte)
  PROBLEMA: Ambele blocaje de imutabilitate au condiţia `new.deleted_at is null`, ceea ce transformă soft-delete-ul într-o portiţă. Scenariu: şoferul face un singur UPDATE pe propria foaie APROBATĂ, cu `deleted_at = now()` şi în acelaşi timp `km_sosire = 999999` — garda „foaia aprobată nu se mai atinge" e sărită complet, iar triggerul AFTER ridică apoi `vehicles.km_curent` la valoarea falsă (care blochează pe vecie foile următoare, fiindcă regresul e refuz explicit). Identic la alimentări: o alimentare de pe o foaie aprobată poate fi ştearsă logic (şi cu litri/cost modificaţi în aceeaşi instrucţiune), deci consumul decontat dispare din rapoarte după aprobare.
  CORECTIE: Scoateţi `new.deleted_at is null` din ambele condiţii şi trataţi ştergerea separat: pe o foaie aprobată refuzaţi orice UPDATE care schimbă altceva decât `observatii`, inclusiv setarea lui `deleted_at` (stornarea trebuie să fie o operaţie explicită, cu drept `trip_sheets:delete`/approve şi motiv). În foi_parcurs_dupa, actualizaţi `km_curent` doar când `new.deleted_at is null`.
- [MARE] supabase/migrations/0012_fleet.sql (vehicule_update, vdoc_update, foi_update, alimentari_update)
  PROBLEMA: Politicile de UPDATE fixează doar `updated_by = auth.uid()`; `created_by`, `created_at` şi — pe trip_sheets — `aprobat_de`/`aprobat_la` rămân la discreţia clientului. Scenariu: şoferul face UPDATE pe propria foaie FĂRĂ să schimbe statusul (rămâne 'aprobat'), deci ramura de tranziţie din trigger nu rulează şi `new.aprobat_de` este exact ce a trimis el — poate pune pe foaie semnătura unui coleg sau a administratorului. Acelaşi mecanism permite rescrierea lui `created_by`/`created_at` pe vehicule, documente şi alimentări, adică falsificarea provenienţei rândului în chiar tabelele auditate.
  CORECTIE: Într-un trigger BEFORE UPDATE comun (sau în cele existente) forţaţi `new.created_by := old.created_by; new.created_at := old.created_at;` şi, pentru trip_sheets, `new.aprobat_de := old.aprobat_de; new.aprobat_la := old.aprobat_la;` ori de câte ori statusul nu se schimbă. Politica nu poate face asta singură (nu are acces la OLD).
- [MARE] supabase/migrations/0012_fleet.sql (app.poate_vedea_vehicul + foi_insert)
  PROBLEMA: `poate_vedea_vehicul` returnează false pentru orice vehicul cu `employee_id IS NULL` la scope 'team' şi 'own', iar `department_id` — coloană existentă, cu index şi cu justificare în comentariu — nu este folosită deloc. Scenariu: maşina de pool a departamentului (cazul descris chiar în comentariul tabelei) este invizibilă pentru şoferi ŞI pentru şeful de departament; le sunt ascunse şi documentele ei (vdoc_select) şi anomaliile ei (anomalii_select). Simetric şi mai grav: `foi_insert` nu verifică deloc vizibilitatea vehiculului, ci doar `poate_vedea_foaie(org, employee_id)` — deci un şofer cu create:'own' poate întocmi foi de parcurs pe ORICE vehicul din organizaţie, inclusiv pe cele pe care nu are dreptul să le vadă, iar la aprobare le ridică `km_curent`.
  CORECTIE: Extindeţi `app.poate_vedea_vehicul(p_org, p_sofer_id, p_department_id)`: la 'team' acceptaţi şi `p_department_id` = departamentul curent al utilizatorului, iar vehiculele de pool (ambele NULL) să fie vizibile la 'team'. Adăugaţi în foi_insert/foi_update condiţia `app.poate_vedea_vehicul(organization_id, app.vehicul_sofer(vehicle_id), ...)` sau, mai simplu, o verificare în internal.foi_parcurs_inainte.
- [MARE] supabase/migrations/0012_fleet.sql (internal.flota_sincronizeaza_grup — `kind` = cod, + vdt_platforma_cod_uq / vdt_org_cod_uq)
  PROBLEMA: `kind` se ia din `vehicle_document_types.cod`, dar codul NU e unic pentru o organizaţie: există un index unic pentru rândurile de platformă şi altul, separat, pentru rândurile proprii. Scenariu: firma îşi creează un tip propriu cu cod 'rca' (permis) şi înregistrează pe acelaşi vehicul o poliţă pe tipul de platformă 'rca' (expiră peste un an) şi una pe tipul propriu 'rca' (expiră peste o lună). Ambele sincronizări scriu pe aceeaşi cheie UNIQUE(organization_id, 'vehicle_document', vehicle_id, 'rca') din expirables: a doua o suprascrie tăcut pe prima, deci una dintre scadenţe dispare complet din dashboardul de conformitate — eşec tăcut, exact clasa de defect pe care restul migrării se străduieşte să o evite. Aceeaşi problemă la redenumirea unui `cod`: expirabilul cu vechiul kind rămâne orfan pe vecie, fiindcă nimic nu resincronizează kind-ul vechi.
  CORECTIE: Faceţi `kind` unic determinist: fie interziceţi printr-un trigger pe vehicle_document_types codurile de organizaţie care se ciocnesc cu un cod de platformă, fie prefixaţi codurile proprii (`kind := case when t.organization_id is null then t.cod else 'org_' || t.cod end`). Blocaţi UPDATE-ul lui `cod` după prima folosire (sau resincronizaţi vechiul kind cu expires_at NULL în vdt-ul AFTER UPDATE).
- [MARE] src/app/(app)/flota/[id]/documente/actions.ts (+ vehicle_documents.fisier_path / fuel_entries.fisier_bon_path în 0012_fleet.sql)
  PROBLEMA: Calea fişierului vine integral de la client (`input.fisierPath`) şi este stocată fără nicio validare de prefix, iar în migrare nu există niciun bucket şi nicio politică pe `storage.objects`. Scenariu: un utilizator din organizaţia A salvează un document de vehicul cu `fisierPath = '<uuid-org-B>/hr/contract-salariu.pdf'`; în momentul în care apare ecranul de descărcare (care va construi inevitabil un signed URL din calea stocată, cel mai probabil cu clientul de serviciu), organizaţia A descarcă un fişier al organizaţiei B. Bon-ul de alimentare are exact aceeaşi problemă.
  CORECTIE: Impuneţi contractul de cale în ambele straturi: în Zod/acţiune generaţi calea serverul (`${ctx.tenant.organizationId}/vehicles/${vehicleId}/${crypto.randomUUID()}`) şi refuzaţi orice cale trimisă de client care nu începe cu `${organizationId}/`; adăugaţi un CHECK pe coloană (`fisier_path is null or fisier_path like organization_id::text || '/%'`) şi politici pe storage.objects cu `(storage.foldername(name))[1]::uuid = any((select app.current_org_ids())::uuid[])` plus aceeaşi verificare de scope ca pe rând.
- [MEDIU] supabase/migrations/0012_fleet.sql (vehicule_update)
  PROBLEMA: Nici USING, nici WITH CHECK nu împiedică schimbarea lui `organization_id` (ambele acceptă orice organizaţie din `app.current_org_ids()`), iar WITH CHECK a pierdut şi `app.feature_on(organization_id, 'fleet')`. Scenariu: un contabil/administrator membru în două organizaţii ale aceluiaşi grup face `update vehicles set organization_id = <org B> where id = <vehicul org A>` — vehiculul migrează în org B (chiar dacă B nu are modulul `fleet` contractat), în timp ce documentele şi foile lui rămân în org A, agăţate de un vehicul pe care triggerele îl vor respinge de acum înainte (`vdoc_inainte`/`foi_parcurs_inainte` compară organizaţiile). Nici `vehicles_dupa` nu urmăreşte organization_id, deci expirabilele rămân în org A.
  CORECTIE: Blocaţi mutarea într-un trigger BEFORE UPDATE: `if new.organization_id <> old.organization_id then raise exception using errcode = 'P0001', message = 'Un vehicul nu poate fi mutat în altă organizaţie.'; end if;`. Adăugaţi `app.feature_on(organization_id, 'fleet')` în toate clauzele WITH CHECK, nu doar în USING.
- [MEDIU] supabase/migrations/0012_fleet.sql (app.vehicul_sofer / app.foaie_sofer)
  PROBLEMA: Ambele sunt SECURITY DEFINER, fără niciun filtru de organizaţie, şi au `grant execute ... to authenticated`. Scenariu: dacă schema `app` este (sau devine) expusă prin PostgREST, orice utilizator autentificat poate apela `app.vehicul_sofer('<uuid vehicul din altă firmă>')` şi primeşte employee_id-ul şoferului — un oracol care confirmă existenţa unui vehicul şi leagă un id de vehicul de o persoană, peste graniţa de tenant. Chiar şi neexpuse, funcţiile încalcă principiul „orice funcţie definer filtrează pe organizaţie".
  CORECTIE: Adăugaţi parametrul organizaţiei şi filtraţi: `select v.employee_id from public.vehicles v where v.id = p_vehicle_id and v.organization_id = p_organization_id` (idem pentru foaie), sau lăsaţi-le fără GRANT către `authenticated` — politicile RLS le evaluează oricum ca proprietar, nu prin dreptul apelantului.
- [MEDIU] supabase/migrations/0012_fleet.sql (anomalii_update + internal.anomalii_protejeaza)
  PROBLEMA: Politica cere doar `app.can(organization_id, 'vehicles', 'update', 'team')`, fără nicio legătură între anomalie şi vehiculul/echipa celui care o atinge, iar WITH CHECK nu fixează `updated_by = auth.uid()` (singura tabelă din migrare unde lipseşte) şi nu interzice `deleted_at`. Triggerul protejează doar datele constatate. Scenariu: şeful de coloană care are `vehicles:update` şi al cărui şofer a produs saltul de kilometraj face `update odometer_anomalies set deleted_at = now()` pe anomaliile oricărui vehicul din firmă — constatarea automată dispare din listă, fără urmă a autorului (updated_by e la alegerea lui).
  CORECTIE: Restrângeţi politica cu `app.poate_vedea_vehicul(organization_id, app.vehicul_sofer(vehicle_id))`, adăugaţi `updated_by = (select auth.uid())` în WITH CHECK şi interziceţi în internal.anomalii_protejeaza trecerea lui `deleted_at` din NULL în non-NULL (o anomalie se confirmă cu notă, nu se şterge).
- [MEDIU] supabase/migrations/0012_fleet.sql (vehicle_document_types.cere_expirare + internal.vdoc_inainte)
  PROBLEMA: `cere_expirare` există în nomenclator, dar nu este verificat nicăieri: nici în trigger, nici în `vehicleDocumentCreateSchema` (unde `expiraLa` e opţional). Scenariu: se înregistrează RCA-ul fără `expira_la`; `flota_sincronizeaza_grup` îl alege drept curent (ordinea are `nulls last`, dar dacă e singurul rând el este curentul) şi apelează `sync_expirable` cu expires_at NULL — poliţa nu produce nicio scadenţă, vehiculul apare „fără scadenţă" (gri) în semafor şi expiră nesemnalat. Câmpul care ar trebui să prevină exact asta e ornamental.
  CORECTIE: În internal.vdoc_inainte: dacă tipul ales are `cere_expirare` şi `new.expira_la is null`, ridicaţi P0001 cu mesaj românesc („Pentru <denumire> este obligatorie data de expirare."). Reflectaţi regula şi în schema Zod, marcând câmpul obligatoriu când tipul selectat are cereExpirare.
- [MEDIU] supabase/migrations/0012_fleet.sql (fuel_entries.pret_litru)
  PROBLEMA: Coloana generată `round(cost / litri, 4)` se evaluează la construirea rândului, ÎNAINTE ca `fuel_litri_ck (litri > 0)` să fie verificat. Scenariu: utilizatorul lasă litri = 0 într-un formular de alimentare şi primeşte în interfaţă eroarea brută PostgreSQL „division by zero" (22012), nu mesajul de business în română — exact ce interzice regula privind mesajele pentru administratorul de firmă, plus scurgerea unui detaliu de implementare.
  CORECTIE: Fie faceţi expresia defensivă (`case when litri > 0 then round(cost / litri, 4) end`), fie validaţi `litri > 0` în triggerul BEFORE `internal.alimentari_inainte` cu errcode P0001 şi mesaj românesc, înaintea evaluării coloanei — în practică, ambele.
- [MEDIU] src/app/(app)/flota/page.tsx (decodificaCursor + ListaVehicule)
  PROBLEMA: Cursorul vine din URL, este decodificat din base64url şi verificat doar cu `typeof === 'string'`, apoi interpolat direct în filtrul PostgREST: `interogare.or(\`created_at.lt.${cursor.createdAt},and(created_at.eq.${cursor.createdAt},id.lt.${cursor.id})\`)`. Scenariu: un utilizator construieşte un cursor cu `createdAt = "x,vin.not.is.null"` şi injectează operatori arbitrari în clauza `or` — poate rescrie filtrarea listei, poate provoca erori 400 exploatate ca oracol boolean pe coloanele vizibile şi transformă un ecran de listare într-un mic motor de interogare. Izolarea între firme rezistă (o dă RLS), dar filtrul aplicaţiei nu mai e al aplicaţiei.
  CORECTIE: Validaţi cursorul cu Zod înainte de folosire: `z.object({ createdAt: z.iso.datetime(), id: z.uuid() })` şi trataţi eşecul ca „fără cursor". Ideal, refolosiţi `codificaCursor`/`decodificaCursor` din @/lib/queries/audit, care există deja în inventar, în loc de o a doua implementare locală.

## produs
VERDICT: Faza 8 NU este livrată — nici tehnic (codul nu compilează, migrarea nu se aplică), nici funcțional (lipsește exact partea pe care o folosesc zilnic șoferii). Din cele două roluri ale lentilei, unul nu are absolut nimic: nu există niciun ecran de foaie de parcurs, de alimentare sau de anomalie de kilometraj — deci nici prepopularea km-ului de plecare (implementată corect în trigger) nu ajunge niciodată la un șofer, iar anomaliile de salt se scriu într-o tabelă pe care nu o citește nicio pagină. Administratorul are trei ecrane, dar nu poate răspunde la întrebarea centrală a modulului („ce document expiră primul, pe ce mașină?”): lista se sortează după data adăugării, semaforul se calculează doar pentru cele 20 de rânduri ale paginii curente, „expirat de 8 luni” și „expiră peste 10 zile” arată identic, iar un vehicul fără niciun document apare gri și liniștitor. Blocantele absolute sunt patru: modulele importate și nelivrate (`@/schemas/fleet`, cele două formulare, `queries/fleet`, `domain/fleet/consum`), funcțiile SQL inexistente `app.can()` și `app.azi_local()` care fac migrarea 0012 să eșueze integral la aplicare, absența completă a UI-ului de foi de parcurs și navigația livrată ca fragment de notițe cu rute care nu există (inclusiv `/flota/nou`, ținta singurului buton din starea goală). Partea de SQL este, altfel, gândită serios — identitatea (vehicul, tip) pentru scadențe și indexul parțial pe `este_curent` rezolvă corect reînnoirea, iar decizia „regresul blochează, saltul avertizează” e bine argumentată; problema e că regresul, așa cum e implementat, face imposibilă tocmai foaia uitată pe care o reclamă anomalia de salt, iar o singură cifră greșită aprobată blochează definitiv vehiculul, fără nicio cale de storno în aplicație. Ca să se poată spune onest că Faza 8 e gata mai lipsesc: modulul de foi de parcurs cap-coadă (mobil-first), ecranul de anomalii cu confirmare, storno pentru foile aprobate cu recalcularea kilometrajului, tratarea vehiculelor de pool în regulile de vizibilitate, obligativitatea datei de expirare acolo unde tipul o cere, evidențierea documentelor obligatorii lipsă, numele reale de șofer și departament în locul UUID-urilor, stările de încărcare/eroare pe ecranele de detaliu, bucket-ul de fișiere semnalat deja și corectarea diacriticelor (ș/ț cu virgulă) în tot textul vizibil.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/page.tsx
  PROBLEMA: Livrarea nu compilează: `@/schemas/fleet` (ETICHETE_CATEGORIE / ETICHETE_STATUS / ETICHETE_COMBUSTIBIL / VEHICLE_CATEGORII / VEHICLE_STATUSURI / vehicleDocumentCreateSchema / vehicleCreateSchema) este importat din 4 fișiere și nu este livrat nicăieri. La fel `../_components/vehicle-form` și `../_components/status-form` (importate în fișa vehiculului), `src/lib/queries/fleet.ts` (anunțat explicit ca „îl continui în răspunsul următor”) și `src/domain/fleet/consum.ts` (există doar un fișier de test trunchiat care îl importă). `next build` cade la prima rezolvare de modul.
  CORECTIE: Livrați efectiv: `src/schemas/fleet.ts` (constante + scheme Zod 4), `src/app/(app)/flota/_components/vehicle-form.tsx`, `status-form.tsx`, acțiunile de vehicul (creare/actualizare/schimbare status), `src/domain/fleet/consum.ts`. Până atunci, Faza 8 nu poate fi declarată livrată nici măcar parțial.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/page.tsx
  PROBLEMA: Nu există niciun ecran de foaie de parcurs. Șoferul — utilizatorul principal al fazei — nu are unde să completeze nimic: nici listă, nici formular de întocmire, nici trimitere spre aprobare, nici ecran de aprobare pentru administrator, nici formular de alimentare, nici listă de anomalii de kilometraj. Din secțiunea „foi-parcurs” s-au livrat doar două funcții de domeniu și testele lor. Consecință directă: motorul de anomalii scrie rânduri în `odometer_anomalies` pe care nu le vede niciodată nimeni, iar `fuel_entries` (obligatoriu legat de `trip_sheet_id`) nu poate fi populat deloc — deci și consumul real afișat pe fișa vehiculului va fi permanent „date insuficiente”.
  CORECTIE: Livrați `/flota/foi-parcurs` (listă + filtre pe status/șofer/vehicul), formularul de întocmire optimizat pentru telefon, acțiunile trimite/aprobă/respinge, formularul de alimentare și ecranul de anomalii cu acțiunea de confirmare. Suplimentar: reevaluați `fuel_entries.trip_sheet_id NOT NULL` — un decont lunar de card de carburant (OMV/Rompetrol/DKV) nu are foaie de parcurs asociată și nu se poate înregistra.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0012_fleet.sql
  PROBLEMA: Migrarea folosește funcții SQL care nu există în inventarul de helperi: `app.can(uuid, text, text, text)` — apelată în 6 politici RLS și în triggerul de aprobare — și `app.azi_local()` (în `internal.vehicles_normalizeaza`). Inventarul confirmă doar `app.has_permission(uuid, text, text)`. `CREATE POLICY` validează referințele la funcții în momentul creării, deci migrarea eșuează la aplicare și NIMIC din Faza 8 nu ajunge în bază. Același risc, nesemnalat, pentru `internal.seteaza_updated_at()` („există din 0008”) și pentru semnătura cu 9 parametri a lui `internal.sync_expirable()`, presupusă, nu citită.
  CORECTIE: Înlocuiți `app.can(org, res, act, scope)` cu o comparație explicită peste `app.has_permission(org, res, act)` (sau creați `app.can` în această migrare, o singură dată, cu `SET search_path = ''`); înlocuiți `app.azi_local()` cu `(now() at time zone 'Europe/Bucharest')::date`; verificați semnătura reală a lui `internal.sync_expirable` și a lui `internal.seteaza_updated_at` în 0008 înainte de a le apela.
- [CRITIC] /Users/maleticimiroslav/ERP Adminio/src/config/navigation.ts
  PROBLEMA: Modulul este inaccesibil din meniu, iar butoanele existente duc în 404. `src/config/navigation.ts` nu este livrat ca fișier, ci ca fragment de notițe („order: /* următorul număr liber */”) care nu se poate salva. Rutele propuse acolo (`/flota/vehicule`, `/flota/foi-parcurs`, `/flota/documente`, `/flota/alimentari`) nu corespund niciunei pagini livrate — lista reală e la `/flota`. În plus, butonul principal „+ Adaugă vehicul” și textul stării goale trimit la `/flota/nou`, care nu există: administratorul cu parc gol dă clic pe singura acțiune sugerată și primește 404.
  CORECTIE: Livrați `src/config/navigation.ts` ca diff real, cu rutele efectiv existente, și creați `/flota/nou` (sau înlocuiți linkul cu un formular pe loc). Verificați fiecare `href` contra structurii din `src/app/(app)/flota/`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/page.tsx
  PROBLEMA: Administratorul nu poate afla „ce expiră primul, pe ce mașină”. Lista e ordonată după `created_at desc`, nu există sortare sau filtru după scadență, iar coloana „Cea mai apropiată scadenţă” e calculată în TS doar pentru cele 20 de rânduri ale paginii curente. La 45 de vehicule, mașina cu ITP expirat poate fi pe pagina 3 și nimic din pagina 1 nu o semnalează. Semaforul este decorativ, nu operațional.
  CORECTIE: Interogați `expirables` (sursa de adevăr a motorului din 0008) cu sortare pe server după cea mai apropiată scadență și adăugați filtrul „expiră în 30 de zile / expirate”; alternativ, materializați `min(expira_la)` pe vehicul și sortați după el în SQL, nu în TS după paginare.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/page.tsx
  PROBLEMA: Semaforul de scadență transmite starea EXCLUSIV prin culoare (WCAG 2.1 SC 1.4.1): badge-ul conține doar data, „12.03.2026”, roșu sau verde. Un utilizator cu deficiență de percepție a culorii, sau oricine se uită la un print alb-negru, nu poate deosebi „în regulă” de „expirat”. Mai grav funcțional: `culoareScadenta` returnează 'rosu' și pentru „expiră peste 10 zile”, și pentru „a expirat acum 8 luni” — două situații total diferite pentru un administrator de flotă (una e o programare, cealaltă e o mașină care circulă neasigurată). În plus, calculul folosește `new Date('YYYY-MM-DD')` (miezul nopții UTC) comparat cu `Date.now()` local, deși proiectul are `todayInBucharest`/`TIMEZONE` în `@/lib/format/date` — un document care expiră azi poate fi raportat ca „mai are o zi”.
  CORECTIE: Adăugați text explicit în badge („expirat de 8 zile” / „expiră în 12 zile” / „valabil”), plus un `aria-label` cu aceeași informație, și extrageți o stare distinctă `expirat`. Calculați diferența de zile din `todayInBucharest()`, nu din `Date.now()`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/documente/_components/document-form.tsx
  PROBLEMA: `cere_expirare` din nomenclator nu produce niciun efect. Prop-ul `cereExpirare` este definit în interfața `TipDocument`, primit din pagină și niciodată folosit; câmpul „Expiră la” nu este `required`, iar migrarea permite `expira_la NULL`. Scenariu: administratorul introduce RCA-ul și uită data de expirare. `internal.flota_sincronizeaza_grup` transmite `p_expires_at = NULL` către `sync_expirable`, scadența dispare logic, alertele se închid, iar în listă vehiculul apare gri, „fără scadenţă”. O poliță RCA fără dată devine risc invizibil, exact opusul scopului modulului.
  CORECTIE: Marcați „Expiră la” ca obligatoriu în UI și în schema Zod atunci când tipul ales are `cereExpirare = true`, și dublați regula într-un trigger BEFORE pe `vehicle_documents` (errcode P0001, mesaj în română): „Tipul de document «RCA» necesită o dată de expirare.”
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/page.tsx
  PROBLEMA: `vehicle_document_types.obligatoriu` este seed-uit (ITP, RCA, rovinietă, stingător, trusă medicală) și nu este citit nicăieri în aplicație. Un vehicul care nu are NICIUN document înregistrat afișează exact același badge gri „fără scadenţă” ca unul fără documente cu termen — adică absența totală a RCA-ului, cea mai gravă situație posibilă, este prezentată neutru, iar mașina nu apare în niciun raport de conformitate.
  CORECTIE: Calculați, pe vehicul, tipurile `obligatoriu = true` fără document curent și afișați-le distinct („lipsă: RCA, ITP”) cu prioritate maximă în sortare; adăugați filtrul „vehicule cu documente lipsă”.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0012_fleet.sql
  PROBLEMA: Vehiculele de pool sunt invizibile pentru toți cei care nu au scope 'all', deși migrarea le declară explicit ca fiind un caz suportat („o maşină de pool nu are şofer nominal”). `app.poate_vedea_vehicul` cere `p_sofer_id is not null` pe ramurile 'team' și 'own', deci un vehicul cu `employee_id = NULL` nu e vizibil niciunui șofer sau șef de echipă. Consecință în lanț: `vdoc_select` și `anomalii_select` folosesc aceeași funcție prin `app.vehicul_sofer(vehicle_id)`, deci nici documentele, nici anomaliile mașinilor de pool nu se văd; iar șoferul care conduce azi o mașină de pool nu o poate selecta în formularul de foaie de parcurs, pentru că nu o vede în listă.
  CORECTIE: Pe ramura 'team' acceptați și `p_sofer_id is null` când vehiculul aparține departamentului utilizatorului (`vehicles.department_id`), iar pe 'own' acceptați vehiculele pentru care utilizatorul are o foaie de parcurs activă; alternativ, tratați explicit vehiculele de pool ca vizibile tuturor membrilor cu `vehicles:read` ≥ 'team'.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/documente/page.tsx
  PROBLEMA: Erorile de interogare sunt înghițite tăcut și se transformă în afirmații false de conformitate. `const { data: documente } = await supabase...` ignoră `error`; la orice eșec (timeout, RLS, coloană lipsă) `documente` este `null` și ecranul afirmă „Niciun document înregistrat încă pentru acest vehicul.” Administratorul citește asta ca „mașina nu are RCA” sau, invers, un ecran gol îl liniștește că nu are nimic de făcut. Același tipar în `[id]/page.tsx` pentru `documente`, `tipuri`, `foi` și `alimentari` (unde afectează și consumul calculat) și în `page.tsx` pentru `documenteCurente` (semaforul devine gri pe toate rândurile).
  CORECTIE: Verificați `error` la fiecare interogare și randați `ErrorState` din `@/components/feedback/error-boundary` cu buton de reîncercare; starea „gol” se afișează numai când `error === null && data.length === 0`.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0012_fleet.sql
  PROBLEMA: O foaie de parcurs uitată nu mai poate fi introdusă niciodată, deși sistemul cere explicit acest lucru. `internal.foi_parcurs_dupa` inserează anomalia cu textul „Verificaţi dacă lipseşte o foaie de parcurs”, dar `internal.foi_parcurs_inainte` refuză orice `km_plecare` sub ultimul kilometraj cunoscut — adică exact foaia care lipsește, a cărei plecare e prin definiție mai mică. Remediul sugerat de mesaj este imposibil de aplicat. La fel, pe o mașină de pool folosită de doi șoferi în aceeași zi, al doilea care își completează foaia seara e respins. Mesajul de eroare recomandă „stornaţi-o şi întocmiţi una nouă”, dar nu există nici status de storno, nici acțiune, nici ecran de storno nicăieri în livrare.
  CORECTIE: Permiteți foi retroactive: verificați regresul doar față de foile care se suprapun în timp cu intervalul declarat (`plecare_la`/`sosire_la`), nu față de maximul absolut; sau adăugați un status `stornat` plus acțiunea corespunzătoare și acceptați foile de completare marcate ca atare, cu anomalie de tip 'regres' informativă în loc de refuz.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0012_fleet.sql
  PROBLEMA: O singură greșeală de tastare aprobată blochează definitiv vehiculul. Scenariu real: șoferul scrie 428000 în loc de 42800 la km de sosire, șeful aprobă. `internal.foi_parcurs_dupa` ridică `vehicles.km_curent` la 428000; nu există nicio tranziție de stare din 'aprobat' (deci foaia nu mai poate fi corectată sau retrimisă), `km_curent` nu scade niciodată prin nicio cale automată, iar baza de comparație e `greatest(max(km_sosire aprobat), km_curent)`. Toate foile ulterioare, corecte, sunt respinse cu „un odometru nu poate da înapoi”. Singura ieșire e ștergerea logică a foii plus corectarea manuală a `km_curent` direct în bază — pentru care nu există nici acțiune, nici ecran.
  CORECTIE: Adăugați tranziția `aprobat → draft` (sau `stornat`) pentru cine are `trip_sheets:approve`, cu recalcularea lui `vehicles.km_curent` din maximul foilor aprobate rămase, și expuneți-o în UI ca „Stornează foaia”. Recalculul trebuie să poată și să coboare `km_curent`, nu doar să-l urce.
- [MARE] /Users/maleticimiroslav/ERP Adminio/supabase/migrations/0012_fleet.sql
  PROBLEMA: Politica `anomalii_insert` este logic nesatisfiabilă: cere simultan `app.is_service_context()` ŞI `organization_id = any(app.current_org_ids())`. Într-un context de serviciu nu există organizație curentă din JWT (deci `current_org_ids()` e gol), iar în sesiunea unui șofer `is_service_context()` este fals. Inserarea anomaliei funcționează astăzi doar pentru că `internal.foi_parcurs_dupa` e SECURITY DEFINER deținută de un rol cu BYPASSRLS — deci politica nu protejează nimic, iar dacă proprietarul funcției se schimbă (alt runner de migrări, alt rol), salvarea unei foi cu salt de kilometraj eșuează cu „new row violates row-level security policy”, un mesaj de nedescifrat pentru șoferul care doar voia să-și încheie ziua.
  CORECTIE: Alegeți o singură gardă: fie `with check ((select app.is_service_context()))` fără condiția pe organizație, fie inserați anomalia printr-o funcție dedicată SECURITY DEFINER cu `GRANT` explicit și interziceți complet INSERT-ul din rolul `authenticated`. Adăugați un test care rulează cu un rol fără BYPASSRLS.
- [MARE] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/page.tsx
  PROBLEMA: Fișa vehiculului afișează identificatori tehnici acolo unde administratorul așteaptă nume: „Şofer alocat: 9f3c1e2a-…”, „Departament alocat: 4b71-…”. Un administrator de flotă nu poate folosi ecranul ca atare — nu știe cine e alocat pe mașină, nu poate căuta după șofer, nu poate schimba alocarea decât dacă are UUID-ul la îndemână. Tot din acest motiv lipsește orice `<select>` cu șoferi în formularul de alocare.
  CORECTIE: Citiți `employees`/`departments` separat (PostgREST nu face join fără FK expus) și construiți în TS o hartă id → nume, cu fallback „nealocat”; expuneți un `<select>` cu numele șoferilor activi ai organizației atât la alocare, cât și în filtrele listei.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/documente/_components/document-form.tsx
  PROBLEMA: Formularul de document are trei defecte de utilizare: (1) costul se parsează cu `Number(cost)`, deci „620,50” — forma normală pe o tastatură românească și pe telefon — devine `NaN` și utilizatorul primește un mesaj de validare generic, deși proiectul are `parseAmount` în `@/lib/format/money`; (2) nu există nicio confirmare de succes după salvare (se cheamă doar `router.refresh()`), deci pe o conexiune lentă utilizatorul apasă a doua oară și înregistrează documentul de două ori — fără constrângere de unicitate care să-l oprească; (3) niciun câmp nu are `required` sau `aria-required`, iar mesajul de eroare nu primește focus.
  CORECTIE: Folosiți `parseAmount` pentru cost și acceptați virgula zecimală; afișați o stare de succes explicită („Documentul a fost înregistrat.”) cu `role="status"`; marcați câmpurile obligatorii și mutați focusul pe blocul de eroare la eșec.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/app/(app)/flota/[id]/page.tsx
  PROBLEMA: Ecranele de detaliu nu respectă setul obligatoriu de stări și se rup pe telefon. Nu există `Suspense`/`SkeletonTable` (spre deosebire de listă), deci pagina rămâne albă cât rulează cinci interogări secvențiale; `throw new Error('Nu am putut încărca vehiculul.')` ajunge în pagina generică de eroare Next pentru că nu e livrat niciun `error.tsx` și nu se folosesc `ErrorBoundary`/`ErrorState`; `section className="grid grid-cols-2 gap-6"` nu are breakpoint, deci pe un ecran de 360px cele două carduri devin ilizibile; tabelele de documente și de foi de parcurs nu au container `overflow-x-auto` (spre deosebire de tabelul din listă), deci pagina însăși derulează orizontal. Antetele de tabel nu au `scope="col"` și tabelele nu au `<caption>`.
  CORECTIE: Împachetați secțiunile în `Suspense` cu `SkeletonTable`, adăugați `error.tsx` pe segmentul `/flota`, folosiți `grid-cols-1 md:grid-cols-2`, puneți fiecare tabel într-un `div` cu `overflow-x-auto` și adăugați `scope="col"` plus `<caption class="sr-only">`.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/src/domain/fleet/kilometraj.ts
  PROBLEMA: Trei praguri de business există în două exemplare care pot diverge. `PRAG_SALT_KM_IMPLICIT = 1500` dublează `internal.flota_prag_salt_km()`, iar helperul client ignoră complet `vehicles.prag_salt_km`: pe un autoturism de oraș configurat cu prag 300, formularul spune „ok” și baza înregistrează totuși o anomalie. `verificaContinuitate` tratează `kmSosire === kmPlecare` ca valid, în timp ce triggerul respinge (`km_sosire <= km_plecare`), deci utilizatorul trece validarea din formular și primește eroare de la server. Pragurile semaforului (14/30 de zile) sunt hardcodate identic în trei fișiere, deși pragurile reale de alertare stau în `alert_rules` — ecranele de flotă vor arăta altceva decât notificările primite pe e-mail.
  CORECTIE: Transmiteți pragul efectiv al vehiculului (`prag_salt_km ?? implicit`) în `verificaContinuitate` și aliniați regula la `<=`; extrageți semaforul într-un singur modul (`src/domain/fleet/scadente.ts`) care citește pragurile din `alert_rules`, și eliminați `codificaCursor`/`decodificaCursor` locale în favoarea celor din `@/lib/queries/audit` sau ale unui helper comun.
- [MEDIU] /Users/maleticimiroslav/ERP Adminio/supabase/seed-flota.sql
  PROBLEMA: Fișiere SQL care nu rulează ca atare, plus tipografie românească greșită în texte văzute de utilizator. (1) `supabase/seed-flota.sql` și `supabase/migrations/0012b_fleet_expirables.sql` încep cu linia `// cale/fisier` — `//` nu este comentariu SQL, psql cade la prima linie. (2) În seed, blocul de gardă `do $$ ... where organization_id = :org_id ... $$` nu funcționează: psql NU interpolează variabile în interiorul șirurilor dollar-quoted, deci serverul primește literalmente `:org_id` și dă eroare de sintaxă. (3) Mesajele de eroare din 0012 folosesc ghilimea de închidere dreaptă în loc de cea românească: `'... din starea „%s" în starea „%s".'` (corect: „%s”), iar întreaga livrare folosește ş/ţ cu sedilă (U+015F/U+0163) în loc de ș/ț cu virgulă (U+0219/U+021B) — „şofer”, „scadenţă”, „Inspecţie tehnică” — adică exact abaterea de la „diacritice corecte” din cerință, în fiecare text vizibil.
  CORECTIE: Înlocuiți antetul cu `--`; scoateți gardajul din blocul `do $$` sau pasați organizația prin `set_config`/parametru; corectați ghilimelele de închidere în ” și rulați o înlocuire globală ş→ș, Ş→Ș, ţ→ț, Ţ→Ț pe tot codul și pe seed.